# 슬래그마 — 도파민 드리븐 관점 게임성 분석 보고서

작성: 2026-09-16 · 대상: `index.html` (v0916_7 기준) · 분석 기준: 코드 실측값 + 기획서 확정 기록

> 이 문서는 기획 결정이 아니라 **분석·제안**이다. 확정 기록(기획서 히스토리)에 올리지 않는다.

---

## 0. 요약 — 한 문장 진단

**이 게임은 도파민 회로의 '각성(arousal)' 단계가 구조적으로 존재하지 않는다.**
보상(강화·소환·정산)은 촘촘하게 깔려 있지만, 그 보상을 **갈망하게 만드는 결핍과 위협**이
코드 레벨에서 자동 해소되도록 짜여 있다. 도파민은 보상 그 자체가 아니라
**"보상 예측 오차(reward prediction error)"** 에서 나오는데, 지금 구조는 예측 오차가 거의 0이다.

---

## 1. 분석 — 사고 공식 적용

선택한 두 공식: **#4 문제 재정의 알고리즘** × **#8 복잡성 해결 매트릭스**

### 1-A. 문제 재정의 — PR = P₀ × T(θ) × S(φ) × M(ψ)

**P₀ (원래 문제)**: "인플레이에 긴장감이 부족하다. 공격받는 것에 대한 두려움이 없다."

#### T(180°) — 반대 관점 회전

"왜 두렵지 않은가"가 아니라 **"이 게임은 플레이어가 두려워할 필요가 없도록 설계되어 있는가"** 를 묻는다.
답은 **그렇다**. 코드가 세 겹으로 두려움을 제거하고 있다.

1. **타깃팅 자동 구조** — `CombatSystem.nearestTargets(count, origin = game.playerPos)`이고
   `playerPos`는 용광로 핵 좌표(220, 315)다. 즉 **핵 앞에 도달해 멈춘 적이 항상 최우선 타깃**이다.
   위기가 발생하는 순간 그 위기의 원인이 자동으로 1순위 사격 대상이 된다.
   → 위기가 플레이어의 개입 없이 스스로 풀린다.

2. **웨이브 타이머의 성과 비연동** — `WaveSystem.update()`는 `waveTimer >= durationSec(12)`이면
   무조건 다음 웨이브로 넘어간다. 못 죽여도 진행된다. **실패에 즉각적 대가가 없다.**

3. **보스 도착 전 사망** — 보스 `travelTimeSec = 10`초이고, 공격은 `e.stopped === true`
   (목적지 도달) 이후에만 발생한다. 기록된 실측은 후반 보스전 평균 6.5초 종료다.
   → **최종보스는 단 한 번도 플레이어를 때린 적이 없다.** 30 WAVE 안의 클라이맥스 7회
   (중간보스 6 + 최종보스 1)가 전부 무피해 구간이다.

#### S(φ=10x) — 확대하여 보기: 보스 HP 공식의 구조적 역전

`computeBossHp()`는 `RunConfig.bossComposition()` → `baseCompositionForStage()`를 쓴다.
그런데 2026-09-04 확정으로 **보스에는 물량 선형 커브(`linearNormalTotal`)를 적용하지 않기로** 했다.
당시엔 "물량 3배 고정" 시절이라 타당한 결정이었지만, 2026-09-08 물량 곡선이
**선형 8→40기**로 바뀌면서 이 예외가 역전을 만들었다.

| WAVE | 구성 | 총 HP | 직전 일반 웨이브 대비 |
|---|---|---|---|
| 27 | 일반 38기 | 188,100 | — |
| **28** | **중간보스 1기** | **93,456** | **49.7%** |
| 29 | 일반 40기 | 193,367 | — |
| **30** | **최종보스 1기** | **117,256** | **60.6%** |

**게임의 감정적 정점이 기계적으로는 가장 쉬운 휴식 구간이다.**
게다가 보스는 단일 대상이라 연쇄·폭발·산탄의 다중처리 가치가 전부 0이 된다 —
플레이어가 런 내내 쌓아 올린 빌드의 절반이 클라이맥스에서 무의미해진다.
"강해졌다"를 증명할 무대에서 강함의 절반이 삭제되는 셈이다.

#### M(ψ = +2) — 메타 레벨 이동: 이 게임의 진짜 문제

한 단계 위에서 보면 문제는 "긴장감"이 아니라 **인과의 단절**이다.

- **화면 위(전장)**: 완전 자동. 플레이어 입력 없음.
- **화면 아래(보드)**: 완전 수동. 전황 정보 입력 없음.

두 영역은 **단방향**으로만 연결된다 — 보드에서 만든 강화가 전장의 DPS 계수가 된다.
역방향(전장 상황 → 보드 행동)은 **에너지 공급 하나뿐**이고, 그마저도
100점 단위 누적이라 특정 순간의 전황과 무관하다.

> 인철 확정(2026-09-16): "이 게임은 디펜스 게임이고 전황을 보여주는 건 시각적 만족감이면서 가장 큰 피드백이야"
>
> **이 확정이 구조와 충돌한다.** 전황은 현재 *피드백*일 뿐 *입력*이 아니다.
> 플레이어는 전장을 **볼 이유가 없다**. 보드만 봐도 최적 플레이가 나온다.
> 전장 면적을 지키는 결정 자체는 옳지만, 그 면적이 지금은 "보기 좋은 스크린세이버"다.

### 1-B. 복잡성 해결 매트릭스 — CS = det|M| × Σ[Si/Ci] × ∏[Ii]

시스템을 5개 하위 시스템으로 분해하고 각각의 **해결 난이도 대비 도파민 기여도**를 본다.

| # | 하위 시스템 (Si) | 복잡도 Ci | 현재 도파민 기여 | 레버리지 |
|---|---|---|---|---|
| S1 | 위협·피격 (EnemySystem·applyDamageToPlayer) | 낮음 | **거의 0** | ★★★★★ |
| S2 | 에너지 경제 (ScoreSystem) | 낮음 | 낮음 | ★★★★☆ |
| S3 | 주문서 공급 (OrderGaugeSystem) | 낮음 | 중간 | ★★★☆☆ |
| S4 | 스킬 (SkillSystem) | 중간 | 낮음 | ★★★★☆ |
| S5 | 메타 성장 (Campaign·Gacha) | 높음 | 중간 | ★★☆☆☆ |

#### S1 — 위협: 기여도 0의 근거

- 핵 HP 1200(Lv.1 노말 4인: 300×4), 방어력 20.
- 스테이지 1 근접 적 ATK: WAVE 1에서 20, WAVE 29에서 48.
- 피해 공식 `max(minimumIncomingDamage=20, atk − def)`.
  → **스테이지 1 전 구간에서 근접 적 피해가 사실상 20~28에 고정**된다.
  20 하한이 방어력 차이를 통째로 삼킨다.
- 기획서 미결 항목에 이미 기록됨: "강철 외피(방어력 +100%)의 효과가 0이며, 스테이지 1은 전 구간이 여기에 해당한다."
- **위기 신호 부재**: 피격 연출은 `coreHitUntil = now + 140ms` 플래시가 전부다.
  저체력 상태 연출·음향·화면 효과가 **하나도 없다**. 도파민의 각성 신호 자체가 없다.

#### S2 — 에너지: "희소"가 아니라 "느림"

| 항목 | 실측값 |
|---|---|
| 런 전체 에너지 | 시작 10 + 지급 140 = **150** |
| 런 길이(30 WAVE) | 약 360초 |
| 에너지 획득률 | **0.42 / 초** |
| 첫 추가 지급 시점 | WAVE 3 종료(누적 108점) |
| 지급 단위 | 5개 덩어리 |
| 홀드 소진 속도 | `holdIntervalSec 0.3` → **1.5초** |

**리듬이 "5개 받고 1.5초 쓰고 40초 기다린다"** 이다. 이건 희소성이 아니라 **공백**이다.
희소성은 "쓸 곳이 많은데 모자란" 상태이고, 공백은 "쓸 게 없는" 상태다. 도파민은 전자에서만 나온다.
오프닝 36초(WAVE 1~3) 동안 플레이어가 할 수 있는 행동은 **총 10번의 생성 + 5번의 머지**가 전부다.

#### S3 — 주문서: 에너지와 정반대 방향으로 과잉

게이지 = 에너지 소모 1점 + 머지 1점, 10점당 1장.
→ 150(에너지) + 약 90(머지) = 240점 → **24장** + 시작 3장 + 중간보스 12장 = **약 39장**.

슬롯은 3개다. 39장이 3칸을 통과해 흐르는데 **폐기 비용이 0**이다(`discard()`는 무조건 성공).
선택지가 많고 버리는 대가가 없으면 그건 선택이 아니라 **소비**다.
*한 재화는 말라 있고(에너지) 다른 재화는 넘친다(주문서). 희소성의 방향이 서로 반대다.*

#### S4 — 스킬: 웨이브 리듬과 위상이 어긋난다

| 스킬 | 쿨타임 | 웨이브(12초)당 사용 |
|---|---|---|
| 분쇄창 | 12초 | 1.00회 |
| 낙뢰 폭격 | 15초 | 0.80회 |
| 슬래그 늪 / 열풍 분출 | 16초 | 0.75회 |
| 굉음 충격 / 재생 불씨 | 18초 | 0.67회 |
| 용광 방벽 | 20초 | 0.60회 |

2개 장착이므로 **웨이브당 약 1.5회**. 도파민 조건 3이 요구하는 "아슬아슬함"이 아니라
**"가끔 생각나면 누르는 것"** 이다. 더 중요한 문제 두 가지:

1. **기회비용이 0이다.** 에너지를 소모하지 않으므로(확정 사항) "지금 쓸까 아껴둘까"의
   판단이 존재하지 않는다. **쿨이 돌면 무조건 누르는 것이 항상 최적**이다.
   의사결정이 아니라 타이머 소화 노동이다.
2. **쿨타임이 실시간이라 웨이브 경계와 계속 어긋난다.** 보스가 4의 배수 WAVE에 온다는 걸
   알아도, 내 쿨이 그때 돌아올지는 누적 오차라 예측 불가다. → 계획이 불가능하면 숙련도가 안 쌓인다.

#### S5 — 메타: 장기 도파민 축의 단절

- **행동력**: 최대 30 / 입장 5 / 5분당 1. → **6판 소진 후 완충까지 150분.**
  스테이지 3 기준 1판 6분이므로 **36분 플레이 → 2.5시간 대기**.
- **골드**: 캐릭터 1명 만렙(Lv.60) = `Σ(50 + 25n)` = **45,725골드**. 4명이면 **182,900**.
  스테이지 3 완주 1판 = 30×14 + 200 = **620골드**. → **1명 만렙에 74판.**
  행동력으로 환산하면 **30시간 이상**. 성장 체감 곡선이 사실상 평평하다.
- **별불**: 스테이지 돌파에서만 250. **스테이지가 막히면 소환이 완전히 정지한다.**
  런의 최대 도파민 스파이크(10연 소환 공개 연출)가 진행 정체와 동시에 죽는다.
  가장 큰 보상이 가장 필요한 순간(막혔을 때)에 차단되는 역설.

#### ∏[Ii] — 상호작용 계수: 세 가지 숨은 연쇄

1. **조기 전환 임계값이 역전되어 있다.** `alive / waveEnemyTotal <= 0.10`.
   - WAVE 1(8기): 0.1×8 = 0.8 → **전멸(0기)해야 조기 전환**.
   - WAVE 29(40기): **4기를 남겨도 조기 전환**.
   → 초반일수록 완벽을 요구하고 후반일수록 관대하다. 난이도 곡선과 정반대다.
2. **강해질수록 런이 짧아지고 총 보상이 준다.** 조기 전환으로 웨이브를 빨리 넘기면
   에너지 수급 절대량은 같지만 **체류 시간이 줄어 머지 횟수가 감소**한다.
   실력의 보상이 "빨리 끝남"이라 성취 피드백이 약하다.
3. **적 방어력 성장 vs 관통·방어 무시 기본값 0.** 스테이지 4부터 적 방어력 +10/스테이지인데
   `defenseIgnore`·`pierceRate` 기본값이 전부 0이고 공급원이 패시브뿐이다.
   → 후반 스테이지에서 빌드가 아니라 **캐릭터 보유 여부**가 벽이 된다. 런의 재미가 로비로 이관된다.

---

## 2. 수용 — 잘 되어 있는 것

비판만 하면 방향을 잃으므로 명시한다. 아래는 **건드리면 안 되는 자산**이다.

1. **머지 → 주문서 → 미사일 강화의 3단 변환**은 구조적으로 훌륭하다.
   티어 합계가 강화 레벨이 되는 규칙은 "지금 쓸까, 더 키워서 쓸까"라는
   **진짜 트레이드오프**를 만든다. 이 게임에서 유일하게 완성된 도파민 장치다.
2. **10점당 1장 분할**(2026-09-15)은 정확한 판단이었다. 30점당 3장의 초반 절벽 제거는
   교과서적인 가변 보상 설계다.
3. **미사일 4종의 다중처리 차별화**(산개/밀집/근접/직선)는 빌드 다양성의 견고한 축이다.
   특히 관통 제거(2026-09-07) 판단 — "수치를 어떻게 조정해도 한쪽이 열등한 버전이 된다" — 는
   정확한 진단이었다.
4. **타격감 계층 분리**(2026-09-15)는 옳다. 판정과 연출을 분리했기 때문에
   아래 제안 대부분을 **밸런스 위험 없이** 적용할 수 있다.
5. **전장 면적 사수 결정**은 옳다. 다만 그 면적이 입력을 받아야 완성된다(제안 14).
6. **`CampaignEconomy`의 순수 함수 분리**는 Unity 이관과 수치 검증 양쪽에 대비한 좋은 구조다.

---

## 3. 제안 — 혁신적 솔루션 15

공식 #5 적용: `IS = Σ[Ci × Ni × Fi × Vi] / Ri`
각 항목에 **실현 가능성(F)** 과 **위험(R)** 을 표기한다.

### 축 A — 두려움을 만든다 (도파민 조건 1)

#### 제안 1. 보스 HP를 물량 곡선에 태운다 ★최우선
`computeBossHp()`가 `baseCompositionForStage()` 대신 `linearNormalTotal()`을 쓰도록 바꾸고,
`midHpExtraMul` 1.0 → **1.6**, `finalHpExtraMul` 1.0 → **2.2**로 올린다.
WAVE 30 보스 117,256 → **약 258,000**. 도달 시간 10초 안에 죽지 않는 것이
"보스가 때릴 수 있게" 만드는 **유일한 전제 조건**이다. 이것 없이는 아래 제안이 전부 무의미하다.
`F: 매우 높음(한 줄 수정) / R: 낮음(에디터 노출값)`

#### 제안 2. 보스 돌진 페이즈 — 접촉을 보장한다
보스 HP가 70% 이하로 떨어지면 남은 거리를 **3초에 주파**하도록
`prepareApproach()`에 HP 연동 재계산을 넣는다.
죽이든 맞든 **반드시 한 번은 접촉**한다. 보스전이 "DPS 체크"에서
**"DPS 체크 + 생존 체크"** 로 바뀐다. 최종보스 ATK 101 vs 방어 20 → 초당 81 피해.
핵 1200 기준 **15초 버티기**가 되고, 여기서 처음으로 방어·회복 스킬이 의미를 갖는다.
`F: 높음 / R: 중간(보스전 난이도 상승 — midAtkMul과 함께 조정)`

#### 제안 3. 핵 균열(Core Fracture) — 위험을 보상 가속으로 바꾼다
핵 HP **35% 이하**에서 상태 진입:
- 화면 가장자리 적색 비네팅 + 심박 사운드 + 핵 스프라이트 균열
- **모든 스킬 쿨타임 -30%**
- **주문서 게이지 획득 2배**
- 골든 피스·크리티컬 머지 확률 2배(8%→16%, 5%→10%)

**위험할수록 강해진다.** 이것이 도파민 설계의 정석인 "각성 → 절정 → 해소" 3단을
처음으로 완성한다. 지금은 각성 신호가 140ms 플래시뿐이다.
연출 계층이 이미 분리되어 있어 판정 위험 없이 붙일 수 있다.
`F: 높음 / R: 낮음(체감 대비 밸런스 영향 작음)`

#### 제안 4. 잔여 적 이자(Leftover Interest) — 실패를 난이도로 누적시킨다
웨이브 전환 시 **미처리 적 1기당 다음 웨이브 적 HP +2%**,
동시에 **그 적들의 처치 점수 +50%**.
실패가 복리로 누적되고, 만회하면 그만큼 보상으로 되돌아온다.
현재는 못 죽여도 아무 일이 없어 **실패라는 개념 자체가 없다**.
`F: 높음 / R: 중간(스노우볼 폭주 방지 상한 필요 — 최대 +40% 권장)`

#### 제안 5. 조기 전환 임계값의 역전을 바로잡는다
`earlyTransitionAlivePct` 고정 0.10을 **절대 수치 기반**으로 바꾼다:
`alive <= max(1, floor(total × 0.08))`.
WAVE 1에서 1기 남겨도 넘어가고, WAVE 29에서는 3기까지만 허용.
초반 관대 → 후반 엄격으로 난이도 곡선과 방향을 맞춘다.
`F: 매우 높음 / R: 매우 낮음`

#### 제안 6. 최소 피해 하한을 방어력 비례로 바꾼다
`max(20, atk − def)`를 **`max(atk × 0.15, atk − def)`** 로 교체한다.
현재 20 하한이 스테이지 1 전 구간의 방어력 차이를 통째로 삼켜
강철 외피(+100% 방어)의 효과를 0으로 만든다(기획서 미결 항목에 기록됨).
비례 하한이면 방어 투자가 **모든 구간에서 항상 15%의 가치**를 갖는다.
`F: 높음 / R: 중간(기존 피격 밸런스 재측정 필요 — 자동 플레이로 검증)`

### 축 B — 재화의 결을 바꾼다 (도파민 조건 2)

#### 제안 7. 에너지 지급을 잘게 쪼갠다
`pointsPerGrant` 100 → **40**, `energyPerGrant` 5 → **2**.
총량은 동일(0.42/초)하지만 **지급 빈도가 2.5배**가 되어
"40초 대기" 리듬이 "16초 대기"로 바뀐다. 10점당 1장 분할(2026-09-15)과 같은 논리다.
`F: 매우 높음(에디터 값 2개) / R: 매우 낮음(총량 불변)`

#### 제안 8. 처치 연쇄 보너스 — 전황을 자원으로 직결한다
**1.5초 안에 5기 처치 시 즉시 에너지 +1** (점수 게이지와 별개).
후반 물량 웨이브에서 폭발·산탄이 잘 터지면 **손에 즉시 보상이 떨어진다.**
지금은 전장에서 일어나는 모든 일이 100점 단위로 뭉개져
"내가 방금 잘했다"는 순간 피드백이 존재하지 않는다.
`F: 높음 / R: 낮음(후반 인플레 주의 — 쿨 1초 권장)`

#### 제안 9. 주문서 폐기에 비용을 붙이고 슬롯을 넓힌다
- 폐기 시 **게이지 -3점** (또는 폐기 후 4초간 재폐기 불가)
- 슬롯 3 → **4**

39장이 3칸을 무료로 통과하는 지금은 "고르는 행위"에 무게가 없다.
버리는 데 값이 붙는 순간 **처음으로 선택이 선택이 된다.**
슬롯 확대는 비교 폭을 넓혀 판단의 즐거움을 보상한다.
`F: 높음 / R: 낮음`

#### 제안 10. 머지 콤보 창 — 확정성을 손속도로 바꾼다
**2.5초 안의 연속 머지**에 게이지 배수: 2연 ×1.2 / 3연 ×1.5 / 4연 ×2.0.
크리티컬 머지(5%)가 "확률로 서프라이즈를 준다"면, 콤보는 **"내가 잘해서 터진다"** 를 준다.
도파민은 우연보다 **자기효능감**에서 더 크고 지속적으로 나온다.
보드 2×8(16칸)은 연쇄 머지를 설계하기에 충분히 좁고 충분히 넓다.
`F: 중간(입력 타이밍 추적 필요) / R: 낮음`

### 축 C — 스킬을 결정으로 만든다 (도파민 조건 3·4)

#### 제안 11. 스킬 쿨타임을 웨이브 12초 격자에 재정렬한다
| 분류 | 현재 | 제안 | 웨이브당 |
|---|---|---|---|
| 공격형(분쇄창·낙뢰) | 12·15초 | **9초** | 1.33회 |
| 제어형(굉음·늪·열풍) | 16~18초 | **14초** | 0.86회 |
| 버프형(칼날·외피) | 11.8초 | 유지 | 1.02회 |
| 회복형(재생·잔불) | 18·12.9초 | **22초** | 0.55회 |

**핵심: 보스 웨이브 진입 시 모든 스킬 쿨타임 30% 즉시 감소.**
→ 일반 웨이브에서는 **아슬아슬하게 모자라고**(조건 3),
보스전에서는 **난사가 가능**해진다(조건 4). 두 조건을 동시에 만족시키는 유일한 해법이다.
제안 1·2로 보스가 실제로 강해진 뒤에만 의미가 있으므로 **반드시 함께 적용**한다.
`F: 매우 높음(수치 변경) / R: 중간(전투력 재측정 항목과 함께 검증)`

#### 제안 12. 스킬에 '과열' 비용을 붙인다 — 기회비용의 부활
에너지 비용은 확정으로 뺐으니 대신 **스킬 사용 시 주문서 게이지 -2점**.

> **"지금 살아남을 것인가, 나중에 강해질 것인가"**

이 한 줄이 스킬을 타이머 소화에서 **실시간 전략 결정**으로 바꾼다.
현재는 쿨이 돌면 무조건 누르는 게 항상 최적이라 의사결정이 없다.
제안 3(핵 균열 시 쿨 -30%)과 맞물리면 "위기에 몰릴수록 미래를 태워 현재를 산다"는
**서사적 긴장**까지 생긴다.
`F: 높음 / R: 중간(게이지 수급과 동시 조정 — 제안 7·8이 선행되어야 함)`

### 축 D — 전장을 입력으로 만든다

#### 제안 13. 조준 지정(Focus Fire) — 가장 높은 레버리지 ★
**전장을 탭하면 3초간 그 지점 반경 60px의 적이 모든 미사일의 우선 타깃이 된다.**
쿨타임 없음, 비용 없음.

코드 변경은 `nearestTargets()`의 `origin` 인자 하나와 탭 핸들러가 전부다.
그런데 효과는 구조적이다 — **화면 위 전투가 처음으로 플레이어 입력을 받는다.**

- 핵 앞 근접부터 지울 것인가, 뒤에서 쏘는 원거리를 끊을 것인가
- 탱커를 먼저 녹일 것인가, 물량을 쓸어담을 것인가
- 보스에게 집중할 것인가, 잡몹을 정리해 에너지를 벌 것인가

**이것 하나로 "전황을 보는 것"이 시각적 만족감에서 실제 플레이로 승격된다.**
인철의 전장 면적 사수 확정이 비로소 완전한 근거를 갖는다.
`F: 매우 높음 / R: 낮음(자동 타깃팅은 기본값으로 유지)`

#### 제안 14. 중간보스 처치 폭발 정산
현재 중간보스는 200점(에너지 10) + 주문서 2장.
여기에 **보드 전체 피스 1티어 일괄 상승**을 얹는다.
6번의 중간보스가 런의 **6개 도파민 스파이크**가 되고,
"보스까지 버티면 도약한다"는 명확한 중간 목표가 생긴다.
현재 런은 30 WAVE 내내 평탄한 기울기라 **기억에 남는 순간이 없다.**
`F: 높음 / R: 낮음(보드 상태만 변경)`

#### 제안 15. 미사일별 실시간 DPS 표시 복원
2026-09-05 확정이 v0914_9에서 누적 레벨 표시로 바뀌고 2026-09-16에 패널째 사라졌다(미결 항목).

도파민 드리븐 게임에서 **"내가 강해지고 있다"는 숫자가 실시간으로 올라가는 것**은
보상 신호 그 자체다. 주문서를 적용하는 순간 좌상단 DPS 숫자가
**튀어오르며 색이 번쩍이는 연출**로 복원한다.
강화의 즉각적 인과를 눈으로 확인시키는 것이 강화 자체보다 중요하다.
`F: 높음 / R: 없음(표시 계층)`

### 축 E — 메타 도파민 축 복구

#### 제안 16. 행동력을 '실패 보호'로 재설계
입장 5 소모를 유지하되 **클리어 시 3 환급**(실패·중도 종료는 전액 소모).
대기 시간이 **실력으로 줄어든다.**
현행(항상 5 소모)은 실력과 무관한 순수 시간 게이트라 숙련의 보상이 없다.
`F: 매우 높음 / R: 낮음(플레이 밀도 상승 — 의도된 방향)`

#### 제안 17. 별불 공급을 스테이지 돌파에서 분리
돌파 250은 유지하고, **"최고 도달 WAVE 갱신 시 별불 10 × 갱신 웨이브 수"** 를 추가한다.

스테이지가 막혀도 조금씩 진전하므로 **소환 루프가 멈추지 않는다.**
현재는 벽에 막히는 순간 가장 큰 보상(10연 소환)이 차단되는데,
이는 **가장 보상이 필요한 순간에 보상을 끊는** 정확히 반대 방향의 설계다.
`F: 높음 / R: 중간(별불 인플레 — 갱신 1회당 상한 필요)`

---

## 4. 적용 우선순위

| 순위 | 제안 | 근거 |
|---|---|---|
| **1** | 1, 2 (보스 HP·돌진) | 다른 모든 제안의 전제. 이것 없이는 긴장감이 물리적으로 불가능 |
| **2** | 13 (조준 지정) | 최소 코드로 구조를 바꾸는 유일한 레버 |
| **3** | 3 (핵 균열) | 각성 신호의 부재를 해결. 밸런스 위험 최저 |
| **4** | 7, 8 (에너지 리듬) | 행동 밀도를 즉시 2.5배로. 총량 불변이라 안전 |
| **5** | 11, 12 (스킬 재정렬·과열) | 조건 3·4를 동시 충족. 1·2 적용 후에만 의미 |
| **6** | 5, 9, 14, 15 | 낮은 위험의 체감 개선 |
| **7** | 4, 6, 10, 16, 17 | 재측정이 선행되어야 하는 항목 |

---

## 5. 측정 제안 — 무엇을 재야 하는가

기획서 "패시브 전면 개편 후 전투력 재측정" 항목과 함께 아래 지표를 자동 플레이로 수집할 것을 제안한다.

| 지표 | 목표값(가설) | 현재 추정 |
|---|---|---|
| 런당 핵 피격 횟수 | **30회 이상** | 0~5회 |
| 보스전 평균 지속 | **12~18초** | 6.5초 |
| 보스 접촉률(보스가 1회 이상 공격) | **100%** | 거의 0% |
| 핵 HP 최저점 | **40~60%** | 90% 이상 |
| 분당 플레이어 입력 | **80회 이상** | 약 52회 |
| 런당 스킬 사용 | **40회 이상** | 약 25회 |

**"런당 핵 피격 0회"는 디펜스 게임에서 가장 심각한 지표다.**
지킬 것이 위협받지 않으면 지키는 행위에 의미가 없다.
