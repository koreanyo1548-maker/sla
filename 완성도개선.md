# Slagma 완성도 개선 — 2026-09-24

CrazyGames 심사에서 전달받은 '완성도 부족' 피드백에 대응하는 구현이다. 세부 심사 보고서는 제공되지 않아, 현재 코드에서 드러나는 화면·아트·조작·피드백 문제를 중심으로 작업했다.

## 반영 내용

### 아트와 화면

- 전투 수호자 24명과 보석·적·코어·무기 아이콘 16개를 새 아틀라스로 제작했다. 기존 캐릭터 순서, 초상화, 로비와 전장 배경에 맞췄다.
- `assets/*_v2.webp`는 실제 알파 채널을 가진 이미지다. 원본 아틀라스는 보존하고 경로표가 새 자산을 선택한다.
- 목업용 이미지 생성 원본의 마젠타 배경을 배포 전에 제거하는 `tools/import-sprites.mjs`를 추가했다. 색키 처리는 런타임에 실행하지 않는다.
- 흑철·금색·불꽃에 맞춰 로비, 전투, 성장, 도감, 팝업, 결과 화면을 정리했다. 스킬은 동일한 SVG 아이콘 체계를 사용한다.
- 가로 화면에서는 전장과 제작 패널을 나란히 배치한다. 작은 데스크톱 창에서는 안내 문구를 줄이고 조작 영역을 압축한다. 세로 화면은 전장 아래 제작 패널을 둔다.
- '준비 중' 유물 메뉴를 내리고 실제 기능 4개를 하단 메뉴에 배치했다.

### 흐름과 조작

- 로비에서 현재 스테이지 위치, 편성, 다음 성장 목표, 출전 버튼을 연결했다.
- 주문서에 무기 이미지, 필요한 보석, 실행 시 소환/강화와 레벨·별을 표시한다. 상세창 버튼을 닫기 기호에서 정보 아이콘으로 바꿨다.
- 스킬에 남은 쿨타임(초), 현재 비용, 에너지 부족·체력 가득·대기 상태를 표시한다.
- 처음 생성기를 누르기 전에는 전투 시간을 멈추고, 실제 행동에 반응하는 튜토리얼에 시각 설명을 추가했다. 건너뛰기를 지원한다.
- 설정, 주문서 상세창, 탭 숨김, 창 포커스 이탈을 각각 일시정지 사유로 관리한다. 모든 사유가 해소되어야 재개한다.
- 키보드: `Space` 생성/길게 생성, `M` 일괄 합성, `Q`/`E` 장착 스킬. 버튼과 보드의 기존 Tab·Enter·Space 조작도 유지한다.
- 실제 이미지 로딩 진행률과 실패 시 다시 불러오기 화면을 추가했다. 이미지 준비 전에는 출전을 잠근다.

### 피드백

- 생성·합성·주문 완료·공격 종류·스킬·핵 피격·보스·성장·승패에 짧은 합성 효과음을 연결했다.
- 사용자 입력 후 오디오를 열며, 설정에서 끌 수 있다. 반복 타격음 간격, 동시 음원 수, 전체 출력 크기를 제한한다.
- 승리와 패배에 맞는 결과 이미지와 다음 행동 문구를 표시한다.
- 새 문구는 한국어·영어를 함께 제공한다. 동작 줄이기 설정도 새 애니메이션에 적용한다.

## 적용 범위

기준 커밋: `d4423b175435ba34a85888791f67d4a5080bd1b0`.

전투 수치, 스킬 비용 규칙, 보상·성장 경제, 세이브 형식은 변경하지 않았다. `balance-editor.html`도 수정하지 않았다. UI 최종 스타일은 `css/quality.css`, 공통 화면 보조 동작은 `js/quality.js`에 있다.

## 실행·확인 상태

`index.html`이 진입점이다. 저장소 루트를 정적 HTTP 서버로 열면 된다. 예: `python3 -m http.server 8000` 실행 후 `http://localhost:8000`.

**플레이 테스트, 자동 테스트, 브라우저 렌더 검증, 제출 빌드는 실행하지 않았다.** `CLAUDE.md`의 '명시적 요청 없는 테스트·검증 금지' 지침을 따랐다. 이번 변경은 심사 통과나 실제 기기 동작이 확인된 제출본이 아니며, 후속 플레이 확인과 최종 제출 판단이 필요한 검토용 구현이다.

플랫폼 어댑터의 CrazyGames SDK 구현은 기존대로 무동작 상태다. 이번 변경으로 SDK 연동이나 재심사 제출을 완료한 것은 아니다.
