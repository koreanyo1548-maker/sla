# 슬래그마 — 도파민 드리븐 관점 게임성 분석 보고서

작성: 2026-09-16 · 대상 커밋 `4649d8c` · 분석 범위 `index.html`(전투·메타 전 계층), `기획서.md`

> ⚠ 이 문서는 **제안·분석**이다. `기획서.md`의 작업 규칙에 따라 여기 내용은 히스토리(확정)에 올리지 않는다.
> 인철이 채택을 확정한 항목만 히스토리 또는 미결 항목으로 옮긴다.

---

## 0. 요약 — 한 문장

**이 게임은 "성장 관리 시뮬레이터"로는 잘 짜여 있지만, "도파민 드리븐 액션"으로는 긴장·희소·쿨타임 세 축이
전부 구조적으로 무력화되어 있다.** 원인은 밸런스 수치가 아니라 세 개의 하드코딩된 구조 상수다:
적 도달시간 10초 고정, 웨이브 길이 12초 고정, 그리고 **처치 점수가 스테이지와 무관한 고정값(일반 3점·중간보스 200점)이라
인플레이 재화 총량이 스테이지 3 이후 완전히 동결된다**는 사실이다.

---

## 1. 분석 — 적용 공식

사고 공식 중 **#2 다차원적 분석 프레임워크(MDA)** 와 **#4 문제 재정의 알고리즘(PR)** 두 개를 혼합했다.
MDA로 도파민이 발생해야 할 층위를 시간·계층·인과 세 차원으로 쪼개 어느 층이 비어 있는지 측정하고,
PR로 "재미가 없다"는 모호한 문제를 관점 회전(θ=180°)·범위 조정(φ)·메타 이동(ψ)으로 재정의했다.

### 1-A. MDA — 시간 차원 (D1)

도파민은 서로 다른 주기의 보상이 겹쳐 울릴 때 발생한다. 이 게임의 주기별 실측은 다음과 같다.

| 주기 | 있어야 할 이벤트 | 현재 실제 | 판정 |
|---|---|---|---|
| 0.3~1초 (미시) | 타격·처치 피드백 | 히트스톱·압축·잔상·파편 4종 구현됨 | **양호** |
| 2~3초 | 피스 생성 → 머지 | 에너지 수입 0.42/초 → **2.4초에 1개** | 빈약 |
| 10~12초 | 웨이브 돌파 | 12초 고정, 30회 반복, 변주 0 | **단조** |
| 12~20초 | 스킬 발동 | 쿨타임만, 코스트 없음 → 무조건 누름 | **선택 없음** |
| 40~60초 | 주문서 완성 = 강화 | 스테이지 3 기준 런당 36장 → 10초에 1장 | 양호 |
| 2~6분 (런) | 클리어·정산 | 골드 정산 + 마일스톤 | 양호 |
| 세션 간 | 소환·성급 | 스테이지 돌파 1회 = 10연 1회 | **막히면 0** |

**진단**: 미시(타격감)와 거시(메타 성장)는 채워져 있는데, 그 사이의 **중간 주기 2~20초 구간이 통째로 비었다.**
도파민 드리븐 게임에서 가장 중요한 건 이 중간 주기다 — "지금 이 순간 내가 뭘 결정했는가"가 사는 층이기 때문이다.

### 1-B. MDA — 계층 차원 (D5) · 인과 차원 (D4)

| 계층 | 위협의 원인 | 플레이어의 대응 | 결과 |
|---|---|---|---|
| 미시 (개별 적) | 적 1기가 10초 걸어와 초당 1회 타격 | 자동 사격이 알아서 처리 | **플레이어 개입 불필요** |
| 중간 (웨이브) | 12초 안에 전체 HP를 녹여야 함 | 생성·머지·주문서 | 개입 있음 — 다만 시간 압박이 아니라 자원 압박 |
| 거시 (보스) | 보스 등장 | — | **보스가 도달하기 전에 죽는다(아래 3-2)** |

**인과 사슬이 끊긴 지점**: `적 등장 → 위협 인지 → 판단 → 행동 → 안도` 이 다섯 단계 중
**"위협 인지"와 "안도"가 존재하지 않는다.** 적 도달은 예고가 없고(텔레그래프 UI 없음), 피격은 조용히 숫자만 깎이며,
막아냈을 때의 안도 연출도 없다. 남는 건 `등장 → 자동처리`뿐이다.

### 1-C. PR — 문제 재정의

- **θ=180° (반대 관점)**: "어떻게 하면 더 재미있게 만들까"를 뒤집으면 →
  **"플레이어가 손을 놓아도 결과가 거의 같은 구간이 어디인가?"** 답: 보스전 전체, 그리고 에너지가 0인 87%의 시간.
  이 두 구간이 전체 플레이 타임의 과반이다.
- **φ 축소(0.1x)**: 1초 단위로 확대해 보면 — 플레이어의 입력은 "생성기 탭"과 "피스 드래그"뿐이고,
  **전투와 보드가 인과적으로 연결되는 지점은 주문서 완료 1회뿐**이다. 전투 화면을 안 봐도 보드만 보면 플레이가 된다.
- **φ 확대(10x)**: 스테이지 1과 스테이지 12의 플레이 경험 차이를 보면 —
  웨이브 구성 동일, 웨이브 길이 동일, 에너지 총량 동일(150), 주문서 수 동일(36장).
  **다른 건 적 HP 숫자와 내 캐릭터 레벨 숫자뿐이다.** 즉 30스테이지 전부가 같은 게임의 배율 조정판이다.
- **ψ=+1 (메타 레벨 상승)**: 이 게임의 진짜 루프는 "전투"가 아니라 "골드로 레벨을 올려 다음 스테이지를 연다"이다.
  전투는 골드 자판기다. 그런데 **자판기는 도파민을 만들지 않는다.**
- **ψ=−1 (하위 개념으로 하강)**: 가장 작은 단위인 "탭 1회"를 보면 —
  생성기 탭은 5색 균등 랜덤이라 **무엇이 나와도 기대 대비 좋거나 나쁘지 않다.**
  골든 피스 8%만이 유일한 서프라이즈이며, 런당 기대 발생 12회(150 생성 기준).

### 1-D. 통찰 도출 (GI 보조 계산)

- O(관찰) 8 — 코드가 매우 투명하고 수치가 한곳에 모여 있어 관찰 난이도가 낮다.
- C(연결) 7 — 머지/주문서/미사일/캐릭터 패시브가 하나의 피해 공식으로 수렴하는 설계는 뛰어나다.
- P(패턴) 9 — "고정 상수 3개가 모든 문제의 상류"라는 패턴이 반복 확인된다.
- S(종합) 8
- A(고정관념) 6 — "머지 디펜스는 이래야 한다"는 장르 관성이 웨이브 12초·자동사격에 강하게 남아 있다.
- B(편향) 4 — 기획서가 미결 항목을 정직하게 기록해 편향이 낮다.

GI = (8×7×9×8)/(6+4) = **403** — 통찰의 여지가 매우 크다는 뜻이다. 문제는 진단이 아니라 **실행 우선순위**다.

---

## 2. 수용 — 잘 되어 있는 것 (건드리면 안 되는 자산)

먼저 비판보다 먼저 인정할 것을 인정한다. 아래는 **이미 도파민 설계로서 옳게 되어 있고, 개선안이 훼손하면 안 되는 부분**이다.

1. **타격감 계층 분리가 정석이다.** `CONFIG.presentation`이 판정과 완전히 분리돼 있고(히트스톱 0.045초,
   처치 0.07초, 보스 0.16초, 히트스톱 쿨타임 0.22초로 물량전 과다 발동 방지), 밀림·압축·잔상·미사일별 파편까지
   4종으로 나뉘어 있다. 이 구조는 유니티 이관 시 그대로 가져갈 가치가 있다.
2. **크리티컬 머지·골든 피스라는 "확률 서프라이즈 주입" 발상 자체가 정확하다.**
   "가장 빈번한 행동에 랜덤이 없다"는 2026-09-02 진단은 도파민 설계의 교과서적 통찰이다. 문제는 발동 밀도(§3-7)뿐이다.
3. **10연 소환 공개 연출**(뒷면 카드 → 등급 빛 예고 → 한 장씩 뒤집기 → 전설 롱 연출)은
   가변 보상 스케줄의 정석 구현이다. 여기는 손댈 필요가 없다.
4. **주문서 = 색·티어 → 레벨**이라는 변환은 훌륭하다. "무엇을 모을지"가 아니라 **"언제 쓸지"**를 묻기 때문에
   머지 보드가 단순 퍼즐이 아니라 자원 타이밍 게임이 된다. 이 코어는 지켜야 한다.
5. **반복형 마일스톤**(A형 증가/B형 고정 혼합)은 런 종료 후 "한 번 더" 동기를 만드는 올바른 장치다.
6. **미결 항목 관리가 정직하다.** 기획서의 [측정 무효]·[재검증 필요] 태깅은 대부분의 프로젝트가 못 하는 수준이다.
   아래 비판 중 절반 가까이는 이미 기획서가 스스로 지적해 둔 것이며, 이 보고서는 그것들을
   **"개별 수치 문제"가 아니라 "세 개의 구조 상수에서 파생된 하나의 문제"로 재배열**한다.

---

## 3. 비판 — 근거와 수치

### 3-1. [긴장감 · 치명] 적 도달시간 10초 고정 vs 웨이브 12초 고정

```js
enemy.base.travelTimeSec: 10          // 모든 타입, 모든 스테이지, 모든 웨이브 동일
types.melee/ranged/tank travelTimeSec: 10 / 10 / 10
waveTiming.durationSec: 12
waveTiming.spawnTimesSec: [0, 1, 2]   // 비율 20% / 30% / 50%
earlyTransitionAlivePct: 0.10
```

- 스폰 물량의 **50%가 t=2초에 나와 t=12초에 도달**한다. 웨이브는 그때 이미 끝난다.
- 즉 정상 플레이에서 **적의 절반은 단 한 번도 공격하지 못한다.**
- 게다가 90%를 처치하면 조기 전환이 걸려 웨이브가 더 빨리 넘어간다 —
  **잘할수록 적이 도달할 기회가 더 줄어든다.** 실력에 대한 보상이 "위협의 소거"라서 긴장 곡선이 평평해진다.
- 타입별 차별화도 없다. 근접·원거리·탱커가 모두 10초에 정지 지점에 선다. 원거리는 `rangePct 0.35`로 더 멀리 서지만
  도달 시간은 같으므로 **체감상 세 타입이 "속도가 같은 색깔만 다른 원"**이다.

**도파민 조건 1(공격받는 것에 대한 두려움) 판정: 미충족.** 두려움의 물리적 근거가 코드에 없다.

### 3-2. [긴장감 · 치명] 보스가 도달하기 전에 죽는다 — 구조적으로 항상

보스 HP는 `computeBossHp()`가 "그 웨이브가 일반 웨이브였다면의 가상 총 HP"로 계산한다.
그런데 가상 구성은 `baseCompositionForStage()`(웨이브 30 기준 22기 상당)인 반면,
실제 직전 일반 웨이브는 `linearNormalTotal()`로 **40기**다.

| 구간 | 직전 일반 WAVE 총 HP | 보스 HP | 비율 |
|---|---|---|---|
| S3 W29 → W30 | 365,248 | 221,483 | **0.61배** |
| S8 W29 → W30 | 741,239 | 449,480 | 0.61배 |
| S12 W29 → W30 | 1,042,032 | 631,877 | 0.61배 |

즉 **최종보스는 직전 일반 웨이브의 61% 체력짜리 단일 표적이고, 이 비율은 스테이지가 아무리 올라가도 변하지 않는다.**
W29를 12초에 클리어할 DPS를 가진 플레이어는 보스를 **7.3초**에 녹인다. 보스 도달은 10초다.
기획서의 실측("후반 보스전 평균 6.5초 종료, 런 전체 보스 피격 0~2회")과 정확히 일치한다.

**런의 감정적 클라이맥스여야 할 보스전이, 구조적으로 "10초 걸어오는 동안 7초 만에 터지는 과녁"이다.**
보스 경보 연출(`midBossAlertSec 0.9` / `finalBossAlertSec 1.1`)이 약속하는 긴장을 게임플레이가 배신한다.
이것이 이 게임의 **단일 최대 결함**이다.

### 3-3. [긴장감] 최소 피격 피해 20 바닥 — 적 타입·스테이지 차이를 지운다

```js
computeIncomingDamage: raw = max(20, 적ATK − 합산방어력)
```

기본 편성(1성 Lv1) 방어력 21, 스테이지 1 근접 적 ATK 20~32.
**전 구간이 최소 피해 20에 걸린다.** 스테이지 12까지도 Lv30·3성 편성(방어 173)이면 적 ATK 122~127이 전부 바닥값이다.

결과:
- 탱커(atkMul 1.3)와 근접(1.0)의 위협 차이가 0이 된다.
- 스테이지 1과 스테이지 12의 "한 대 맞았을 때의 무게"가 동일하다.
- 강철 외피(방어력 +100%)가 대부분 구간에서 **효과 정확히 0**이다.
- 기획서도 이미 [플레이 검증]으로 지적해 둔 항목이다. 다만 원인은 "스킬 형태"가 아니라 **절대값 바닥값**이다.

### 3-4. [긴장감] 적 방어력 성장은 사실상 존재하지 않는 축

`defenseConstant: 1000`에 대해 적 방어력은 스테이지당 +10 선형이다.

| 스테이지 | 적 방어 | 근접 피해 감쇠 | 탱커 감쇠 |
|---|---|---|---|
| S4 | 10 | ×0.990 | ×0.985 |
| S12 | 90 | ×0.917 | ×0.881 |
| S30 | 270 | ×0.787 | ×0.712 |

30스테이지를 가도 최대 21% 감쇠다. `defenseIgnore` 팩터(상한 90%)와 방어 무시 패시브(키라 14%, 볼칸 10%)가
붙어봐야 **체감이 불가능한 축**이다. 성장 선택지 하나가 사실상 죽어 있다.

### 3-5. [재화 · 치명] 인플레이 재화 총량이 스테이지 3 이후 완전 동결

```js
scoring: { normalKill: 3, midBoss: 200, pointsPerGrant: 100, energyPerGrant: 5, startEnergy: 10 }
```

처치 점수가 **적 HP·스테이지와 무관한 고정값**이고, 개체 수는 WAVE 번호로만 결정된다.
따라서 30웨이브 스테이지는 스테이지 번호와 무관하게 **항상 동일한 총 점수 2,876점**을 낸다.

| 스테이지 | 적 총 HP | 총 에너지 | 총 주문서 |
|---|---|---|---|
| S1 (10W) | 186,437 | 40 | 13장 |
| S3 (30W) | 4,101,658 | **150** | 36장 |
| S8 (30W) | 8,323,953 | **150** | 36장 |
| S12 (30W) | 11,701,789 | **150** | 36장 |

**스테이지 12의 벽은 스테이지 3의 2.85배인데, 그 벽을 넘기 위해 런 안에서 쌓을 수 있는 힘은 1.00배다.**

이 구조의 필연적 귀결:
- 런 내부 성장은 **천장이 고정**되므로 모든 진행이 **로비 성장(캐릭터/스킬 레벨)에만** 의존한다.
- 그래서 전투는 "이번 판에 어떤 빌드가 나올까"가 아니라 **"내 레벨이 충분한가"의 확인 절차**가 된다.
- 기획서가 말한 검증 목표 — *"한 판 안에서 액티브 스킬 조합과 머지 운영으로 빌드가 만들어지는 재미"* — 가
  **구조적으로 검증 불가능한 상태**다. 빌드의 최대 크기가 스테이지와 무관하게 고정이기 때문이다.

### 3-6. [재화] "넉넉하면서 희소"가 아니라 "대부분 빈손, 가끔 폭식"

```js
generator: { costPerPiece: 1, holdIntervalSec: 0.3 }   // 홀드 시 3.33개/초 생성 가능
```

- 스테이지 3 기준 에너지 수입은 **0.42/초**. 생성 능력은 3.33/초. → **공급이 소비 능력의 12.5%**.
- 즉 생성기는 플레이 시간의 **약 87%를 비활성(disabled)** 상태로 있는다.
- 지급은 100점 단위로 **5개씩 뭉쳐서** 오고, 5개는 1.5초면 전부 소진된다.
- **저축의 긴장이 0이다.** 에너지의 유일한 소비처가 생성기 하나이므로, 들어오면 즉시 쓰는 것이 언제나 최적해다.
  선택이 없는 자원은 희소성이 아니라 그냥 대기시간이다.

**도파민 조건 2(넉넉하면서 희소성 유지) 판정: 희소하지만 넉넉하지 않고, 무엇보다 선택이 없다.**

### 3-7. [재화] 서프라이즈 밀도 부족

- 크리티컬 머지 5% × 런당 머지 ~75회 = **기대 3.75회**
- 골든 피스 8% × 런당 생성 150회 = **기대 12회**
- 둘 다 pity 없음(주문서 3성에만 `qualityPityMisses: 12` 존재)
- 크리티컬 머지는 `canMerge`가 최대 티어를 제외하므로 **T3→T4 구간에서 보너스 티어가 버려진다**
  (`clamp(a.tier+bonus, 0, maxTier-1)`). 가장 짜릿해야 할 순간에 보상이 조용히 삭제된다.

### 3-8. [스킬 · 치명] 코스트 없는 쿨타임 = 판단 없는 버튼

```js
skillPickCount: 2
cooldownSec: 12(분쇄창) / 15(낙뢰) / 16~20(제어·버프) — 에너지 코스트 없음
REQUIRES: enemy(적 있음) / missingHp(HP 안 참) — 그 외 조건 없음
```

- 웨이브가 12초인데 쿨타임이 12~20초다. → **웨이브당 정확히 1회.** 리듬이 아니라 메트로놈이다.
- 코스트가 없으므로 **"지금 쓸까, 아껴둘까"의 판단이 존재하지 않는다.** 쿨이 돌면 누르는 것이 항상 최적해다.
- 지속형 스킬은 더 심각하다:
  ```js
  cooldown = 6성최대지속 / (1 − durationUptimeGap 0.15)
  ```
  → **6성 기준 가동률 85%.** 달군 칼날·강철 외피·치유 잔불은 성장 완료 시 **사실상 상시 발동**이다.
  "아슬아슬한 쿨타임"의 정반대 방향으로 성장이 설계돼 있다.
- 제어 스킬 3종(굉음 충격·슬래그 늪·열풍 분출)은 **제어할 위협이 없다.**
  적이 도달조차 못 하는데 스턴 2초·넉백 45px가 무슨 의미인가. 11종 중 3종이 설계 전제부터 공회전한다.

**도파민 조건 3(강력하되 쿨타임이 아슬아슬) 판정: 미충족. 강력하지도, 아슬아슬하지도 않다.**

### 3-9. [스킬] 조건 4의 정면 위반 — 난사도 안 되고 적도 약하다

사용자 조건 4: *"스킬을 난사 가능하게 하려면 적이 충분히 강해야 한다."*
현재는 **난사가 불가능하면서(쿨 12~20초) 동시에 적이 약하다(도달 전 사망).**
두 축이 모두 낮은 쪽에 있어, 어느 방향으로도 긴장이 만들어지지 않는 **사분면의 죽은 구석**에 위치한다.

```
        적 강함
          ↑
   난사형 │ 하드코어
  (조건4) │
──────────┼──────────→ 스킬 난사도
   [현재] │ 자원관리형
          │
        적 약함
```

### 3-10. [보상 곡선] 첫 강화가 DPS 하락일 수 있다

```js
applyUpgrade(): this.basicActive = false;   // 미사일 1개라도 켜지면 기본 공격 즉시 정지
```

시작 주문서 3장은 미사일 4종 중 랜덤 3종의 공격력이다. 단일 대상 기준 첫 강화 직후 배율:

| 첫 강화 미사일 | Lv.1 | Lv.2 | Lv.3 |
|---|---|---|---|
| 폭발 (1.20) | 1.32 | 1.42 | 1.50 |
| 연쇄 (1.00) | 1.10 | 1.18 | 1.25 |
| **산탄 (0.85)** | **0.94** | 1.00 | 1.06 |
| **레이저 (0.85)** | **0.94** | 1.00 | 1.06 |

산탄(직선탄 1기 명중)·레이저로 시작하면 **첫 보상이 단일 대상 DPS 6% 하락**이다.
물량이 모이면 회복되지만, 스테이지 1 웨이브 1(8기가 넓게 분산)에서는 회복되지 않는다.
**튜토리얼 구간에서 첫 보상이 처벌로 체감될 수 있다** — 도파민 설계에서 가장 피해야 할 순간이다.

### 3-11. [메타] 행동력 제약이 이미 해제되어 있다

```js
charge(s){ s.stamina = min(30, s.stamina + 5); s.staminaChargeClicks++; return ...; }
```

**충전 버튼에 비용이 없다.** 골드도 광고도 아무것도 소모하지 않고 무한히 +5를 누를 수 있으며,
누른 횟수만 카운트한다. 즉 행동력 시스템은 현재 **UI로만 존재하고 게임에는 영향이 없다.**
(프로토타입 편의일 가능성이 높지만, 현 상태로는 "희소 재화"라는 설계 의도가 0이다.)

### 3-12. [메타] 막히면 모든 수도꼭지가 동시에 잠긴다

```js
milestones.stage_clear: { target:1, reward:250, currencyId:'starfire' }
gacha: { cost:200, resultsPerPull:10, startStarfire:0 }
```

별불은 **"새로운 최고 스테이지 클리어"에서만** 나온다. 스테이지가 막히면:
- 별불 수입 0 → 소환 0 → 신규 캐릭터/스킬 0 → 조각 0 → 성급 0
- 남는 건 골드 레벨업뿐인데, 그건 가장 느린 선형 성장(`50 + 25n`)이다.

**벽에 부딪힌 플레이어에게 남는 유일한 도파민 경로가 가장 지루한 경로다.** 이탈 지점이 여기다.

### 3-13. [리듬] 30웨이브 전부 같은 웨이브다

`buildStageWaves()`가 만드는 30개 웨이브의 차이는 **개체 수(8→40)와 난이도 계수**뿐이다.
길이 12초 동일, 스폰 타이밍 [0,1,2] 동일, 배치 비율 [0.2,0.3,0.5] 동일,
조기 전환 10% 동일, 보스는 4의 배수 고정.
**플레이어가 "이번 웨이브는 다르다"를 느낄 수 있는 변주가 0개다.**

### 3-14. [피드백] 위협 인지·안도의 연출이 없다

- 적이 공격 위치에 도달하기 전 **텔레그래프가 없다.** (보스 경보는 등장 시점 1회뿐)
- 피격은 `fx-shake` + 숫자 하나. **"막아야 한다"는 신호가 아니라 "이미 맞았다"는 사후 통보**다.
- 웨이브 종료 시 잔적이 다음 웨이브로 그대로 넘어가는데(`enemies` 배열은 웨이브 전환 시 비워지지 않음),
  이 **누적 침투가 UI에 전혀 표시되지 않는다.** 플레이어는 자신이 왜 죽었는지 모른다.
- 핵 HP는 스킬 없이는 **런 내내 절대 회복되지 않는다.** 누적 드레인 구조 자체는 좋으나,
  가시화가 없어 "천천히 조여오는 공포"가 아니라 "모르는 사이 흐르는 누수"가 된다.

### 3-15. [메타] 결과 화면에 기록 경신이 없다

`renderResultStats()`는 도달 웨이브·시간·점수·주문서·머지·스킬 사용을 나열한다.
**최고 기록 대비 표시도, 경신 연출도, 이전 판 대비 증감도 없다.**
패배했을 때 "다시 한 번"을 눌러야 할 이유가 골드 말고는 없다.

---

## 4. 제안 — 혁신적 솔루션 (IS = Σ[조합 × 참신성 × 실현성 × 가치] / 위험)

우선순위는 **IS 값 기준**이다. 각 항목에 `[영향]/[난이도]`와 수정 지점을 명시했다.

---

### 제안 1. 도달시간을 "위협 다이얼"로 승격 — 10초 고정 해제 `[영향 최상] / [난이도 하]`

**핵심 통찰**: 이 게임에서 긴장을 만드는 유일한 물리량은 `적 도달시간 ÷ 웨이브 길이`다.
현재 10/12 = 0.83으로, 적이 도달하는 순간 웨이브가 끝난다. 이 비율만 내려도 게임이 달라진다.

- `travelTimeSec`을 타입별로 분화: **근접 7초 / 원거리 9초 / 탱커 11초**.
  세 타입이 서로 다른 시점에 압박을 시작하므로, 웨이브 안에 **3단 리듬**이 생긴다.
- 웨이브 후반 스폰(t=2, 물량의 50%)에는 `travelTimeSec × 0.7` 보정 —
  **마지막에 들어온 적이 가장 빨리 도달**해 웨이브 종료 직전이 가장 뜨거워진다.
- 신규 타입 **돌격병(rusher)**: HP 0.5배, 도달 4초, 처치 점수 5점.
  "지금 당장 처리해야 하는 적"이라는 개념이 게임에 처음 생긴다.
- 조기 전환 조건을 `alive ≤ 10%`에서 **`도달 전 적만 남았을 때`**로 바꾼다.
  → 잘하면 웨이브가 빨리 끝나는 게 아니라 **"위협을 완전히 차단했을 때"** 끝난다. 실력의 보상이 소거가 아니라 성취가 된다.

위험: 초반 난이도 급등. → 스테이지 1~2는 기존 10초 유지, 스테이지 3부터 적용하는 램프를 둔다.

---

### 제안 2. 보스를 "과녁"에서 "타이머"로 — 보스전 재설계 `[영향 최상] / [난이도 중]`

세 가지를 동시에 바꿔야 보스전이 산다.

1. **보스 도달시간 10 → 3초.** 보스는 걸어오는 존재가 아니라 **이미 와 있는 존재**여야 한다.
2. **보스 HP 산식 교체.** 현재 "가상 일반 구성"은 직전 웨이브의 0.61배다.
   `직전 3개 일반 웨이브 평균 총 HP × 1.6`으로 바꾸면 보스전이 **15~20초**로 고정된다.
   (단일 대상이라 광역기 손실이 있으므로 체감 길이는 더 길어진다 — 이건 의도된 효과다.)
3. **보스에게 위협 사이클을 준다.** 패턴 AI를 만들 필요는 없다. 기존 시스템 재사용으로 충분하다:
   - `bossChargeSec: 4` — 4초마다 핵을 향해 **충전 → 2초 텔레그래프 → 광역 타격(최소피해 무시, 핵 최대HP의 8%)**.
   - 텔레그래프 2초 동안 **제어 스킬(스턴·감속·넉백)로 취소 가능**하게 한다.
   - 이 한 줄로 §3-8의 죽은 제어 스킬 3종이 **"보스전 전용 카운터"라는 명확한 존재 이유**를 얻는다.
   - 스킬 아이콘이 처음으로 "쿨이 돌아서 누르는 것"이 아니라 **"지금 눌러야 하는 것"**이 된다.

**이것이 이 보고서에서 가장 중요한 제안이다.** 보스전이 살아나면 런 전체의 감정 곡선이 생긴다.

---

### 제안 3. 최소 피격 피해를 비율형으로 — 한 줄로 4개 문제 해결 `[영향 상] / [난이도 최하]`

```js
// 현재
minimumIncomingDamage: 20
// 제안
raw = max(핵최대HP × 0.006, 적ATK − 합산방어력)
```

이 한 줄이 동시에 고치는 것:
- 적 타입별 위협 차이가 살아난다 (탱커 atkMul 1.3이 실제로 아프다).
- 스테이지가 올라갈수록 "한 대의 무게"가 커진다.
- **강철 외피·용광 방벽이 전 구간에서 유효해진다** (기획서 [플레이 검증] 항목 해결).
- 캐릭터 방어력 성장이 "최소 피해에 먼저 닿아 무의미해지는" 함정에서 벗어난다.

계수 0.006이면 기본 편성(핵 HP 1,230) 기준 7.4 피해 → 현재 20보다 낮으니 **초반은 오히려 완화**되고,
Lv30·3성(핵 HP ~6,000)에서는 36 → **성장할수록 위협도 함께 커지는 올바른 곡선**이 된다.

---

### 제안 4. 처치 점수를 스테이지에 연동 — 인플레이 성장 천장 해제 `[영향 최상] / [난이도 하]`

§3-5의 동결 구조를 푸는 가장 작은 변경:

```js
// 방안 A (권장): 점수를 적 최대 HP에 비례
points = ceil(enemy.maxHp / (기본HP × 0.3))     // 스테이지·타입·난이도가 자동 반영

// 방안 B (보수적): 스테이지 계수만
scoring.normalKill = 3 × (1 + 0.20 × (stageId − 1))
```

방안 A의 부수 효과가 특히 좋다 — **탱커(hpMul 2.0)를 잡으면 점수도 2배**가 되므로,
"어떤 적을 먼저 죽일 것인가"라는 **타겟 우선순위 판단**이 처음으로 게임에 생긴다.
현재 `getNearestEnemy` / `nearestTargets` 자동 조준에는 이 판단이 통째로 빠져 있다.

효과: 스테이지 12에서 총 에너지가 150 → 약 420이 되어, **런 내부 빌드의 최대 크기가 스테이지와 함께 자란다.**
기획서의 검증 목표("한 판 안에서 빌드가 만들어지는 재미")가 비로소 측정 가능해진다.

---

### 제안 5. 에너지에 두 번째 소비처 — 저축의 긴장 만들기 `[영향 상] / [난이도 중]`

에너지가 지루한 이유는 부족해서가 아니라 **쓸 곳이 하나뿐**이기 때문이다. 소비처를 3개로 늘린다.

| 소비처 | 비용 | 효과 | 만들어지는 판단 |
|---|---|---|---|
| 피스 생성 (기존) | 1 | 색 피스 1개 | 기본 |
| **긴급 과열** | 5 | 3초간 전 미사일 공격속도 +100% | "지금 태울까, 모아서 강화할까" |
| **주문서 리롤** | 8 | 슬롯 1칸의 주문서를 새로 뽑음 | "이 조합을 기다릴까, 바꿀까" |
| **핵 응급 수복** | 12 | 핵 HP 15% 즉시 회복 | "죽지 않으려면 성장을 포기" |

이 순간부터 에너지는 **"성장 vs 생존"의 환율**이 된다. 위기일수록 성장을 포기해야 하고,
안전할수록 더 빨리 성장한다 — **리스크-리워드 곡선이 처음으로 생긴다.**
(제안 1·2로 위협이 실재하게 된 뒤에 도입해야 의미가 있다.)

---

### 제안 6. 과열(오버차지) — 조건 4의 정면 구현 `[영향 상] / [난이도 중]`

사용자 조건 4를 그대로 시스템화한다: **"적이 강하면 난사할 수 있어야 한다."**

- 모든 스킬 쿨타임을 **현재의 40%**로 단축 (12초 → 5초, 20초 → 8초).
- 대신 **과열 게이지** 추가: 스킬 1회당 +25, 초당 −8 자연 냉각.
- 게이지 100 도달 시 **3초간 전 스킬 잠금 + 미사일 공격속도 −50%**.
- **핵심 장치**: 적 처치 시 과열 −3 (보스 −20).
  → **적이 많고 강할수록 냉각이 빨라져 난사가 가능해진다.** 조건 4가 수치로 구현된다.

이 설계의 우아함: 한가한 구간에서는 난사가 불가능하고(냉각원이 없음),
물량·보스 구간에서만 난사가 허용된다. **긴장과 화력이 같은 방향으로 움직인다.**

---

### 제안 7. 지속형 스킬의 가동률 상한을 뒤집는다 `[영향 중] / [난이도 최하]`

```js
// 현재: 6성 가동률 85% → 상시 발동
cooldown = 최대지속 / (1 − 0.15)
// 제안: 성급이 오를수록 "더 강하게, 더 짧게"
durationUptimeGap: 0.15 → 0.45          // 6성 가동률 55%
// + 성급은 지속시간이 아니라 효과 강도를 올린다 (growth: 'duration' → 'effect')
```

**성장의 방향을 "상시화"에서 "피크의 강화"로 바꾼다.**
6성 달군 칼날이 "항상 +25%"가 아니라 "5초간 +45%"가 되면,
**언제 터뜨릴지가 실력**이 된다. 이것이 "아슬아슬한 쿨타임"의 본질이다.

---

### 제안 8. 위협 텔레그래프 3종 `[영향 상] / [난이도 하]`

두려움은 피해가 아니라 **예고**에서 온다. 판정은 건드리지 않고 표시 계층만 추가한다.

1. **도달 예고**: 적이 공격 위치 1.5초 전에 진입하면 적↔핵 사이에 **붉은 조준선**이 그어진다.
   (`prepareApproach`에서 이미 `attackPos`를 알고 있으므로 계산 불필요)
2. **침투 카운터**: 화면 상단에 `⚠ 침투 3`. 잔적이 누적되고 있다는 사실을 처음으로 보이게 한다.
   3기 이상이면 핵 테두리가 붉게 맥동한다.
3. **핵 위험 상태**: HP 30% 이하에서 화면 가장자리 비네트 + 심박 SFX + `hitStopCooldownSec` 완화.
   **저체력 구간이 감각적으로 다른 게임이 되어야 한다.**

§3-14의 "모르는 사이 흐르는 누수"가 "조여오는 공포"로 바뀐다.

---

### 제안 9. 웨이브 리듬 변주 — 30개의 같은 웨이브를 깬다 `[영향 상] / [난이도 중]`

`buildStageWaves()`에 웨이브 타입을 3종 추가한다. 물량 총합은 유지하고 **배치만 바꾼다.**

| 타입 | 빈도 | 규칙 | 감정 |
|---|---|---|---|
| **러시** | 5의 배수 | 길이 6초, 물량 동일, 전원 t=0 동시 스폰 | 순간 폭발 |
| **정예** | 7의 배수 | 1~3기, 개체 HP ×8, 처치 점수 ×8 | 단일 화력 시험 |
| **봉인** | 11의 배수 | 생성기 사용 불가, 대신 종료 시 에너지 +20 | 보유 자원 운영 |

**봉인 웨이브가 특히 중요하다.** 생성기를 막는 순간 플레이어는 처음으로
**전투 화면을 봐야만 한다** — §1-C에서 지적한 "보드만 봐도 플레이가 되는" 문제의 직접 해법이다.

---

### 제안 10. 스테이지 모디파이어 — 30스테이지에 30개의 얼굴 `[영향 중] / [난이도 중]`

스테이지마다 규칙 1개를 고정 부여한다(로비에 미리 표시 → 편성 판단의 근거가 된다).

- `적 이동속도 +35%` / `핵 방어력 −50%` / `보라 피스 미출현(4색 운영)` /
  `주문서 슬롯 2칸` / `원거리 적 비율 2배` / `중간보스 2기 동시` …

편성·스킬 선택이 처음으로 **스테이지별로 달라질 이유**를 갖는다.
현재는 어떤 스테이지든 최적 편성이 하나뿐이라 로비 화면이 "숫자 올리기"에 머문다.

---

### 제안 11. 첫 강화의 역행 제거 `[영향 중] / [난이도 최하]`

§3-10의 "첫 보상 = 처벌" 문제. 세 가지 중 택일:

- **A (권장)**: `basicActive`를 즉시 끄지 않고 **첫 미사일 활성 후 1웨이브 동안 병행 유지**.
  "새 무기가 추가되었다"는 순수 덧셈 체험이 된다. 첫 강화의 체감이 가장 좋아지는 방안.
- **B**: 산탄·레이저 `baseDamageMul` 0.85 → **1.00**. (기획서 [재검증 필요] 항목과 함께 처리)
- **C**: 시작 주문서 3장을 랜덤 3종이 아니라 **폭발·연쇄 우선**으로 고정(상승폭이 확실한 미사일).

---

### 제안 12. 서프라이즈 밀도 2배 + 상한 돌파 `[영향 중] / [난이도 하]`

```js
criticalMerge: { chance: 0.05 → 0.09, bonusTiers: 1, pityMerges: 15 }   // 15회 미발동 시 확정
goldenPiece:   { chance: 0.08 → 0.12, bonusTiers: 1, pityGenerates: 12 }
```

그리고 **가장 중요한 수정**:
```js
// 현재: T3→T4 크리티컬 머지의 보너스 티어가 조용히 삭제된다
b.tier = clamp(a.tier + bonus, 0, maxTier - 1);
// 제안: 상한에 걸리면 버리지 말고 "각인 피스"로 전환 — 주문서 레벨 계산 시 +2 추가
```
**가장 짜릿해야 할 순간(이미 최고 티어)에 보상이 사라지는 것은 도파민 설계의 정반대다.**

---

### 제안 13. 기록 경신 루프 — 패배에도 보상을 `[영향 중] / [난이도 하]`

결과 화면에 다음을 추가한다:
- `최고 도달 웨이브 24 → **27** 🔥 NEW` (경신 시 전용 연출 + 별불 소량 지급)
- `최대 강화 레벨 합 / 최단 클리어 시간 / 최대 단일 피해` 3개 트랙
- **경신 시 별불 +10** → §3-12의 "막히면 모든 수도꼭지가 잠기는" 문제를 부분 해소.
  벽에 부딪혀도 **"더 깊이 갔다"는 진전**이 별불이 되어 소환으로 이어진다.

---

### 제안 14. 타겟 우선순위를 플레이어에게 `[영향 중] / [난이도 중]`

현재 조준은 전부 자동(`getNearestEnemy` / `nearestTargets` / `getHighestHpEnemy`)이다.
전투 화면에 플레이어의 의사결정이 **0개**인 근본 원인이다.

- 적을 탭하면 **집중 표적** 지정 → 모든 미사일이 3초간 그 적을 우선 조준.
- 제안 4(HP 비례 점수)와 결합하면 **"탱커를 먼저 녹여 에너지를 당길까, 근접을 쳐내 침투를 막을까"**라는
  실시간 판단이 생긴다. 코드 변경은 `nearestTargets`에 우선순위 필터 한 겹뿐이다.

---

### 제안 15. 행동력 — 제약으로 살리거나 깔끔히 없애거나 `[영향 하] / [난이도 최하]`

§3-11. 현재 무료 +5 버튼은 설계 의도를 무효화한다. 둘 중 하나로 정리해야 한다.
- **살린다**: 충전 비용 `골드 200 × (1 + 0.5 × 누적충전횟수)` 부여. 골드에 성장 외 소비처가 생긴다
  (기획서가 지적한 "levelCostMul의 근거가 사라졌다" 문제와도 맞물린다).
- **없앤다**: 프로토타입 단계에서 검증 대상이 아니므로 UI에서 제거. 어중간한 현 상태가 최악이다.

---

### 제안 16. 웨이브 간 핵 회복 — 누적 드레인에서 승부 단위로 `[영향 중] / [난이도 하]`

현재 핵 HP는 런 내내 회복되지 않아 **초반 실수가 후반에 조용히 처형**된다(지연 처벌 = 학습 불가).

- 웨이브 클리어 시 **핵 HP 8% 회복**. 단, **잔적 0기로 클리어했을 때만.**
- → 각 웨이브가 **독립된 승부 단위**가 되고, "완전 차단"에 명확한 보상이 붙는다.
- 회복이 생기므로 §3-1·3-3의 위협 상향을 **안전하게 더 세게** 걸 수 있다. 제안 1~3의 리스크 완충재다.

---

## 5. 실행 순서 제안

구조 상수를 건드리는 순서가 중요하다. 아래 순서가 아니면 밸런스 측정이 서로를 오염시킨다.

| 단계 | 항목 | 이유 |
|---|---|---|
| **0** | 자동 플레이 기준선 재측정 | 기획서 [측정 무효] 해소. 이게 없으면 이후 전부 추측이다 |
| **1** | 제안 4 (점수 스테이지 연동) | 재화 동결이 풀려야 다른 모든 밸런스가 의미를 가진다 |
| **2** | 제안 3 (최소 피해 비율형) · 제안 11 (첫 강화 역행) | 한 줄 수정, 즉시 효과, 리스크 최소 |
| **3** | 제안 1 (도달시간 분화) · 제안 16 (웨이브 회복) | 위협과 완충을 **반드시 함께** 넣는다 |
| **4** | 제안 2 (보스 재설계) | 최대 효과. 3단계로 위협 기준선이 잡힌 뒤에 |
| **5** | 제안 8 (텔레그래프) · 제안 12 (서프라이즈) | 연출 계층, 판정 무관, 언제든 가능 |
| **6** | 제안 6 (과열) · 제안 5 (에너지 소비처) · 제안 7 (가동률) | 스킬 축 전면 재조정. 위협이 실재한 뒤에만 유효 |
| **7** | 제안 9 (웨이브 변주) · 제안 10 (모디파이어) · 제안 13·14 | 리듬·다양성·리텐션 |
| **정리** | 제안 15 (행동력) | 판단만 하면 되는 항목 |

---

## 6. 도파민 4조건 최종 판정

| 조건 | 현재 | 원인 | 해결 제안 |
|---|---|---|---|
| **1. 인플레이 긴장감** | ❌ 미충족 | 도달 10초 / 웨이브 12초 / 보스 도달 전 사망 / 최소피해 20 바닥 | 1, 2, 3, 8, 16 |
| **2. 넉넉하되 희소한 재화** | ⚠ 절반 | 총량 스테이지 무관 동결 / 소비처 1개 / 87% 빈손 | 4, 5, 12 |
| **3. 아슬아슬한 쿨타임** | ❌ 미충족 | 코스트 없음 / 웨이브=쿨타임 / 6성 가동률 85% 상시화 | 6, 7 |
| **4. 난사 ↔ 강한 적** | ❌ 미충족 | 난사 불가 + 적 약함 (사분면 죽은 구석) | 6 (+ 1, 2) |
| **(추가) 리듬 변주** | ❌ | 30웨이브 전부 동일 구조 | 9, 10 |
| **(추가) 리텐션** | ⚠ | 막히면 별불 0 / 기록 경신 없음 | 13 |

---

## 7. 마지막 한 마디

이 프로토타입의 코드 품질과 문서화 수준은 매우 높다. 시스템 분리, 단일 피해 계산기, 정의 테이블 디스패치,
연출/판정 분리 — 전부 정석이다. **문제는 "잘 만든 것"이 아니라 "무엇을 만들고 있는지"다.**

현재 슬래그마는 **자원 관리 시뮬레이터**다. 플레이어는 보드를 보고, 색을 맞추고, 주문서를 누른다.
전투 화면은 그 결과가 재생되는 창이다. 이것도 하나의 완결된 장르이고, 실제로 잘 작동한다.

하지만 도파민 드리븐을 지향한다면 필요한 건 **"플레이어가 전투 화면을 봐야만 하는 순간"**이다.
지금 그 순간은 정확히 0개다. 제안 1·2·9(봉인 웨이브)·14는 전부 같은 하나의 목표를 향한다 —
**플레이어의 눈과 손을 보드에서 전장으로 끌어오는 것.**

그리고 그 모든 것의 앞에 제안 4가 있다. **런 안에서 성장할 수 있는 최대치가 고정된 게임은,
아무리 위협을 올려도 "내 레벨이 충분한가"라는 질문 하나로 수렴한다.** 재화 동결을 먼저 풀어야 한다.
