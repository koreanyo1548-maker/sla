# 슬래그마 · 연출 개선안 (타격감 · UI/UX 피드백)

작성일: 2026-09-18 · 기준 커밋: `be91f1e` (PR #18) 시점의 `index.html`
이 문서는 인플레이 타격감과 로비·성장 콘텐츠의 UI 연출을 점검한 결과와 개선 계획이다.
`개발계획서.md`의 세션 흐름 안에서 **연출 세션 A(인플레이)** 와 **연출 세션 B(로비·성장)** 로 실행하고,
사운드·질감은 `기획서.md` 로드맵 6단계(아트·사운드)에서 이어 간다.

## 이 문서를 쓰는 방법

- "현재 위치"의 줄 번호는 기준 커밋 시점 값이다. 리팩토링(개발계획서 세션 1)으로 파일이 나뉘면 줄 번호는 무효가 되므로 **섹션 마커 · 함수/객체 이름 · CSS 클래스 이름**으로 찾는다. 줄 번호는 참고일 뿐이다.
- 모든 연출은 **표시 계층에만** 둔다. 판정·밸런스(`CONFIG` 중 `presentation` 외 항목, 적 좌표, HP 계산)는 건드리지 않는다. 이건 2026-09-15 확정 원칙이다.
- 수치는 `CONFIG.presentation`(히트스톱·밀림·압축·잔상 등)이나 `VISUAL_CONFIG`(파티클 수·흔들림) 또는 CSS 변수에 두어 밸런스 에디터의 타격감 섹션에서 조정할 수 있게 한다.
- `prefersReducedMotion()`을 지킨다. 새 움직임 연출은 전부 이 분기 아래에 넣는다.
- 위치 계산은 캔버스 논리 좌표(440×480)나 DOM 요소 상대 좌표만 쓴다. 화면 크기·비율이 달라져도 깨지지 않아야 한다. DOM 플로트는 기존 `combatLayerPoint()`를 그대로 쓴다.
- 사운드 호출 지점은 연출 세션 A·B에서 미리 심되(`GameAudio.play(kind)` 호출만), 실제 소리는 로드맵 6단계에서 만든다.

---

## 1. 현재 연출 계층 지도 (기준 커밋)

어디에 무엇이 있는지 먼저 안다. 아래는 이번 점검에서 확인한 실제 위치다.

### 1-1. 설정

| 항목 | 위치 | 내용 |
|---|---|---|
| 타격감 수치 | `config.js` 섹션 · `DEFAULT_CONFIG.presentation` (약 1821~1841행) | 히트스톱 3종(치명 0.045 · 처치 0.07 · 보스 0.16초)과 배속 0.08, 쿨다운 0.22초, 밀림 10px·복귀 0.18초, 압축 0.16·0.12초, 잔상 0.22초, 보스 알럿·승패 슬로우 |
| 파티클·흔들림 | `config.js` 섹션 · `VISUAL_CONFIG` (약 1921~1927행) | 최대 파티클 180, 피격 5·처치 14·스킬 28, 피격 시 흔들림 on |
| 스킬 연출 종류 | `config.js` 섹션 · `SKILL_DEFS[*].fx` (약 1522행~) | `lance` `bombard` `shield` `heal` `energy` `aura` `control` 7종 |

### 1-2. 인플레이 · 캔버스 연출

| 항목 | 위치 | 현재 동작 |
|---|---|---|
| 연출 엔진 | `heroes.js`/`combat.js` 경계 · `class VisualEffects` (약 2297행) | 파티클·링·번개·빔·잔상 큐와 `update/draw` |
| 적 피격 반응 | `VisualEffects.enemyHit / hitDirection / hitBurst` (약 2340~2385행) | 흰 플래시 85ms, 발사 방향 밀림+압축, 미사일별 파편(연쇄 스파크·폭발 충격파·산탄 원뿔·레이저 잔광) |
| 피해 숫자 합산 | `VisualEffects.queueDamage / flushDamage` (약 2386~2412행) | 0.14초 창에서 공격 종류·치명·관통별 합산, `spawnDamageStack`으로 DOM 표시 |
| 처치 | `VisualEffects.enemyKilled` (약 2413~2426행) | 파편·링·잔상(보스 1.8배), 보스는 흔들림 |
| 플레이어 피격 | `VisualEffects.playerHit` (약 2431~2436행) | 붉은 파편, `#combat-wrap.fx-player-hit` 비네트, `#hud .hp-wrap.fx-hp-hit`, 흔들림 |
| 영웅 발사 | `VisualEffects.playerShot` (약 2427행) + `HeroFieldSystem.draw`의 `shot`·`pulse` (약 2276~2285행) | 파편 3개, 스프라이트 4px 반동 180ms, 강화 시 0.55초 글로우 |
| 스킬 연출 | `VisualEffects.SKILL_FX` (약 2442~2488행) | fx 종류별 링·파편·번개·플레이어 오라, 공격 스킬은 흔들림 |
| 히트스톱 | `game.js` 섹션 · `Game.hitStop / timeScale` (약 4722~4732행) | 치명·처치·보스 처치에서 호출, 쿨다운으로 물량전 보호 |
| 적 그리기 | `EnemySystem.draw` (약 3437~3475행) | 밀림·압축·바운스·그림자·HP바·상태이상 글자·CC 아이콘. 표시 좌표만 바꾸고 `e.x/e.y`는 불변 |
| 적 근접 공격 | `EnemySystem.update` (약 3423~3434행) | `attackCd`가 0이면 **즉시** `applyDamageToPlayer`. 연출 없음 |
| 적 원거리 공격 | 같은 곳 + `CombatSystem.update` 투사체 분기 (약 3195~3206행) | 투사체가 핵 표면에 닿으면 소멸·피해. 발사·도달 연출 없음 |
| 핵 | `core.js` 섹션 · `CoreField` (약 2106행~) | 위치·표면 계산만. 피격 반응·HP 상태 없음 |
| WAVE 시작 | `WaveSystem.startWave` (약 3502~3536행) | 플레이어 위 링 1개, 배지 갱신, 보스면 `showBossAlert` |
| WAVE 배지 | `WaveSystem.renderWaveTypeBadge` (약 3538행) + CSS `#wave-type-badge` (약 966~973행) | 글자 교체만 |
| 보스 알럿·승패 | `Game.showBossAlert` (약 4507행) + CSS `#cinematic-layer` (약 145~180행) | 밴드·카드·스크림, 승패는 슬로우·카메라 푸시·입력 잠금 |

### 1-3. 인플레이 · DOM 연출 (CSS 클래스 → 호출 위치)

| 클래스 | CSS 위치 | JS 호출 | 비고 |
|---|---|---|---|
| `.piece.fx-spawn / fx-merge / fx-critical / fx-invalid` | 약 264~267행 | `MergeBoard.render`가 `cell.fx` 값으로 부여 | 머지는 스케일 팝만 |
| `#generator-btn.fx-generate` | 약 223행 | `Generator` (약 3854행) | |
| `#generator-btn.fx-energy-grant`, `#energy-display.fx-gain`, `#energy-status-row.fx-gain` | 약 224·233·234행 | `Game.updateEnergyUi` (약 4583~4591행) | |
| `.order-gauge-wrap.fx-charge / fx-full` | 약 251~252행 | `OrderGaugeSystem.render` | |
| `.order-card.fx-complete` | 약 271행 | **호출 없음** | CSS만 있고 `OrderSheetSystem.complete`(약 3990~4010행)는 카드를 즉시 비운다 |
| `.order-card.fx-invalid` | 약 272행 | `OrderSheetSystem.complete` 실패 분기 | |
| `.skill-btn.fx-use / fx-ready` | 약 194~195행 | `Game` 스킬 버튼 핸들러 (약 4620·4635행) | |
| `#action-log.fx-log` | 약 277행 | `logAction` (약 2124행) | 개발 표시용. 출시 빌드에서 빠질 예정 |
| `.float-num`, `.damage-stack` | 약 300~306행 | `spawnFloatNumber / spawnDamageStack` (약 2151~2177행) | DOM 요소, 700ms 후 제거 |

### 1-4. 로비·성장 연출

| 항목 | 위치 | 현재 동작 |
|---|---|---|
| 공통 피드백 | `art.js` 섹션 · `GameFeedback.toast / burst` (약 2048~2052행) | 토스트 1칸(`#lobby-notice`, 덮어씀), `.growth-burst` 테두리 글로우 + 비프 |
| 사운드 | `GameAudio` (약 2032~2046행) | 오실레이터 비프 `tap / up / reveal` 3종, 기본 꺼짐 |
| 레벨업·승급 | `CharacterLobbyUI` 모달 핸들러 (약 5374~5379행), `SkillLobbyUI` (약 5540행) | 둘 다 `GameFeedback.burst` + 토스트. 승급·패시브 해금 구분 없음, 전후 수치 없음 |
| 편성 | 같은 곳 (약 5380~5383행) | 비프 + 토스트 |
| 재화 표시 | `CampaignView.wallet` (약 5600행~) | `textContent` 교체. 변화 연출 없음 |
| 마일스톤 수령 | `MilestoneSystem.claim` (약 5485~5491행), `MilestoneUI` (약 5544행~) | 토스트만. 수령 가능 뱃지 없음 |
| 결과 화면 | `Game.renderResultStats` (약 4778행), CSS `#result-title` `resultReveal` (약 282·318행) | 제목 팝인 하나, 나머지는 즉시 표시 |
| 소환 공개 | `GachaLobbyUI.reveal` (약 5635행~), CSS `.pull-slot` `pullPulse` `pullBurst` `pullRise` (약 828~849행) | 카드 뒤집기·등급 맥동·전설 섬광·요약. **잘 돼 있음** |
| 소환 상자 | CSS `.gacha-chest` (약 691행) → 테마 오버라이드 (약 802행) `animation:none` | 부유 애니메이션이 꺼져 있음 |
| 로비 홈 | CSS `.forge-beacon` `forgePulse` (약 624~625행) | 용광로 맥동만 |
| 스테이지 헤더 | `CampaignView.stageHeader` (약 5613행~) | 텍스트 교체. 해금 연출 없음 |

---

## 2. 진단 요약

### 인플레이 · 잘 돼 있는 것
적 피격 4종 구분, 히트스톱과 쿨다운, 처치 잔상, 피해 숫자 합산, 스킬 7종, 보스 진입·승패 시네마틱, 보드·게이지·에너지·스킬 버튼 애니메이션. 전부 판정과 분리돼 있고 reduced-motion을 지킨다. 기반은 좋다.

### 인플레이 · 비어 있는 것 (심각한 순)
1. **적 공격이 보이지 않는다.** 근접은 즉시 피해, 원거리는 탄만 난다. 예비동작·타격 연출이 없어 방어 스킬 타이밍을 배울 수 없다.
2. **핵이 반응하지 않는다.** 모든 적의 목표인데 피격·HP 단계·저HP 경고가 없다.
3. **주문서 완료 연출이 사실상 없다.** CSS `orderComplete`는 있으나 호출이 없고 카드는 즉시 사라진다. 영웅 쪽 0.55초 펄스만 있어 카드→영웅 인과가 안 보인다.
4. **WAVE 클리어 비트가 없다.** 조기 전환은 배지 글자만 바뀐다.
5. **보스 HP가 HUD에 없다.** 스프라이트 위 58px 바뿐이다.
6. 영웅 발사 순간이 약하다(4px 반동, 파편 3개). 상태이상은 글자뿐이다.
7. 사운드가 0이다.

### 로비·성장 · 비어 있는 것
1. **레벨업과 승급이 같은 연출.** 3성 패시브 해금도 강조 없음. 능력치 전후 값 없음.
2. **재화가 그냥 바뀐다.** 카운트업·글로우 없음. 마일스톤 수령이 토스트 한 줄.
3. **결과 화면이 제목 팝인 하나.** 통과 WAVE·골드·최고 기록이 순차 없이 한 번에. 스테이지 돌파 별불은 로비 마일스톤에서 따로 받아 "돌파" 감각이 결과 화면에서 끊긴다.
4. 소환은 강하다. 부족한 건 NEW/중복 구분과 중복 시 조각 진행도, 꺼진 상자 애니메이션.
5. 새 스테이지 해금·편성 변경·마일스톤 수령 가능 상태 표시 없음.
6. 토스트 1칸이라 연속 행동에서 덮인다.

---

## 3. 연출 세션 A · 인플레이 가독성

**목적**: 판정 무관, 캔버스·DOM 표시 계층만으로 "누가 왜 때렸고, 무엇이 강해졌는지"가 보이게 한다.
**시점**: 개발계획서 세션 2(파일 분할·어댑터) 뒤. 세션 4 전이어도 되고 후여도 된다.

### A-1. 적 공격 텔레그래프

- [x] **근접 예비동작**: `EnemySystem.update`에서 `attackCd`가 예비 구간(참고 0.25초)에 들어가면 `e.fxWindupAt`을 찍고, `EnemySystem.draw`에서 그 구간 동안 스프라이트를 핵 쪽으로 기울이며 1.0→1.12 확대한다. 타격 순간 원래 크기로 스냅.
  → 필드 이름은 `e.fxWindup`이고 타임스탬프가 아니라 0~1 진행도다(`1-attackCd/windupSec`). `draw`가 그 값에
  비례해 기울기·확대를 한 번에 계산하므로 draw 쪽에서 시간을 다시 재지 않는다. 이동 중에는 0으로 되돌려
  멀어진 적이 예비 자세로 굳지 않게 했다.
- [x] **근접 타격 임팩트**: `applyDamageToPlayer` 호출 직전에 `VisualEffects.coreImpact(point, dir)`를 부른다. 핵 표면에 링(반지름 참고 18)과 적→핵 방향 파편 4~6개.
  → 순서가 중요하다. 피해를 먼저 주면 그 피해로 패배 연출이 시작된 뒤에 링이 떠서 어긋난다.
- [x] **원거리 발사**: 투사체 push 시 적 위치에 머즐 플래시(작은 링 0.12초). 투사체에 3~4프레임 트레일(이전 위치 배열, `VisualEffects` 큐 또는 투사체 객체의 `trail`).
  → 트레일은 투사체 객체의 `p.trail`이다. 기록은 `CombatSystem.update`의 filter 맨 앞에서 한 번만 하므로
  아군·적 투사체가 같은 경로를 쓴다. 그리기는 탄보다 먼저 깔아 탄이 위에 오게 했다.
- [x] **원거리 도달**: 핵 표면 소멸 시 같은 `coreImpact`. 방향은 탄의 진행 방향에서 가져온다.
- [x] 수치는 `CONFIG.presentation`에 `windupSec` `coreImpactRadius` `projectileTrailLen`으로 추가.
  → 11개를 넣었다: `windupSec` `windupScale` `windupLeanPx` `coreImpactRadius` `coreImpactParticles`
  `coreFlashSec` `coreStages` `coreLowPct` `projectileTrailLen` `muzzleFlashSec`. 배열인 `coreStages`만
  빼고 9개가 밸런스 에디터의 새 "인플레이 연출" 섹션에 나온다(입력 423 → 432개).

### A-2. 핵 반응과 HP 단계

- [x] **피격 반응**: `coreImpact`에서 핵에 흰 플래시 80ms + 압축(적 피격과 같은 `squash` 파라미터 재사용). 핵 그리기 함수(`CoreField` 또는 `Game.render`의 핵 분기)에 표시용 필드를 얹는다.
  → `GameArt.drawCore`가 `game.coreFlashUntil` · `game.coreSquashAt` · `game.coreStage`를 읽는다.
  스프라이트를 흰색으로 덮으면 모양이 사라지므로 얇은 흰 테두리를 얹는 방식으로 했다.
- [x] **HP 단계**: 공용 HP 비율 70 / 40 / 15%에서 핵 외형을 단계별로 바꾼다(균열 라인 추가, 불꽃 크기·밝기 감소). 단계 경계는 `CONFIG.presentation.coreStages=[.7,.4,.15]`.
  → 균열은 고정 각도 3줄(`GameArt.CORE_CRACKS`)이라 프레임마다 흔들리지 않는다. 단계마다 한 줄 늘고
  불투명도가 11%씩 내려간다. 경계 100/75/70/50/40/25/15/8%를 훑어 단계 0/0/1/1/2/2/3/3 확인.
- [x] **저HP 경고**: 15% 이하에서 `#combat-wrap` 가장자리 붉은 맥동(CSS 클래스 `is-critical`, 1.2초 주기)과 HUD HP바 점멸. 회복으로 15%를 넘으면 해제.
  → `#combat-wrap`의 `::before`·`::after`가 이미 배경 그라디언트와 피격 비네트를 쓰고 있어
  전용 요소 `#core-alert`를 뒀다(셰이크 애니메이션과도 겹치지 않는다). HP바는 `.hp-wrap` 자체가
  `fx-hp-hit` 애니메이션을 쓰므로 안쪽 `#player-hp-bar`만 점멸시킨다. 30%로 회복 시 해제 확인.
- [x] 사운드 호출 지점: `core_hit`, `core_low`(진입 1회). `coreLowAnnounced` 플래그로 진입 때만 부른다.

### A-3. 주문서 완료 → 영웅 연결

- [x] `OrderSheetSystem.complete`에서 카드 DOM에 `fx-complete`를 부여하고 **약 0.3초 뒤에** 슬롯을 비우고 `render()`한다. 지금은 즉시 비운다. 그 사이 입력은 해당 카드만 막는다.
  → 실제 함수 이름은 `resolveSlot`이다. 새 `playCompletionFx`로 분리했다. 지연 동안 슬롯이 폐기·재충전으로
  바뀌었으면 손대지 않는다(`this.slots[slotIdx]!==slot` 확인). 입력 차단은 `.is-resolving`.
  reduced-motion이거나 카드 DOM을 못 찾으면 예전처럼 즉시 비운다 — 연출이 진행을 막아서는 안 된다.
- [x] 카드 위치(`getBoundingClientRect` 중심)에서 해당 미사일 영웅 위치(`heroField.origin(module)` → `combatLayerPoint`)로 글로우 파티클 6~8개가 0.35초에 날아가는 DOM 또는 캔버스 연출. 캔버스 좌표계 밖(보드 영역)에서 출발하므로 DOM 플로트 계층(`floatLayer`)이 편하다.
  → **`floatLayer`를 쓸 수 없었다.** `floatLayer`는 `#combat-wrap`이고 그 요소는 `overflow:hidden`이라
  보드에서 출발하는 연출이 잘린다. 전장과 보드를 함께 덮는 `#fx-layer`를 새로 뒀다(`#app` 안, `inset:0`,
  `pointer-events:none`). 도착점은 `combatLayerPoint(#combat-wrap)`으로 캔버스 배율을 반영해 구한 뒤
  두 요소의 rect 차이로 `#fx-layer` 좌표로 옮긴다 — 화면 비율이 달라져도 그대로 맞는다.
  스파크 7개, CSS 변수(`--spark-dx/dy/color/dur`)로 목표 델타를 넘긴다.
- [x] 도착 시 영웅 `upgradedAt` 펄스를 0.55→0.9초로 늘리고(`HeroFieldSystem.draw`의 `pulse` 분모), 영웅 위에 `공격력 Lv.3` 형식 플로트(`spawnFloatNumber`, 새 클래스 `upgrade`).
  → 스파크 도착 시점(350ms)에 `upgradedAt`을 다시 찍어 펄스가 도착에 맞춰 시작한다.
- [x] 문자열은 개발계획서 세션 3 이후라면 `t()`로, 그 전이라면 기존 `enhancementLabel` 결과를 그대로 쓴다.
  → 세션 3 전이므로 `enhancementLabel` 결과를 그대로 쓴다(`연쇄: 공격력 Lv.3` 형식).
- [x] 사운드 호출 지점: `order_complete`. 기존 `up` 비프를 이 kind로 바꿨다.

### A-4. WAVE 클리어 비트

- [x] `WaveSystem.update`의 조기 전환·전멸 판정 지점에서 `renderWaveTypeBadge` 앞에 배지에 `fx-clear` 클래스(0.5초 팝 + "CLEAR" 캡션 교체)를 준다.
  → `playWaveClearBeat()`. 진행 저장(`host.progress`)이 성공한 뒤에 부르고, 마지막 WAVE는 건너뛴다
  (클리어 시네마틱이 받는다). 캡션은 520ms 뒤 "WAVE"로 되돌린다.
- [x] 다음 WAVE로 배지 글자가 바뀔 때 슬라이드 인(CSS `fx-next`).
  → `renderWaveTypeBadge(cfg, slide)`에 인자를 추가했다. 첫 WAVE(`idx===0`)에는 주지 않는다.
- [x] 보스 WAVE 진입은 기존 `showBossAlert` 유지. `boss_alert` 사운드 지점만 앞에 붙였다.
- [x] 사운드 호출 지점: `wave_clear`.

### A-5. 보스 HUD HP바

- [x] `#hud` 아래 또는 `#combat-wrap` 상단에 `#boss-hp` 바. 보스 스폰 시 0→현재 비율로 0.6초 채우고, 처치 시 바가 좌우로 갈라지며 사라지는 CSS.
  → `#combat-wrap` 상단에 뒀다. 중간보스 WAVE로 확인 — 진입 100% → 35% 피해 반영 → 처치 시
  `is-defeated` 애니메이션 후 0.6초 뒤 숨음.
- [x] 갱신은 `EnemySystem.update` 끝에서 보스 하나만 찾아 width 갱신(매 프레임 DOM 쓰기 1회, 허용 범위).
  → `syncBossHpBar()`. 보스 id가 바뀔 때만 스폰 연출을 걸고 그 밖에는 width 하나만 쓴다.
- [x] 스프라이트 위 58px 바는 유지.
- [x] ~~가로 레이아웃(세션 4)에서는 위치만 CSS로 분기.~~
  → 가로 레이아웃이 취소돼(2026-09-18) 분기할 것이 없다. `#combat-wrap` 안의 오버레이 그대로 둔다.

### A-6. 발사 순간과 상태이상

- [x] **머즐 플래시**: `VisualEffects.playerShot`에 미사일 색 링(반지름 10, 0.1초) 추가. 파편 3→5.
  → `playerShot(moduleKey, origin)`으로 인자를 받게 하고 `HeroFieldSystem.shot`이 여기에 위임한다.
  전에는 두 곳에 각자 emit이 있어 영웅이 있을 때와 없을 때 연출이 달랐다 — 이제 한 구현이다.
- [x] **투사체 트레일**: 연쇄·폭발·산탄 투사체에 `trail` 배열(길이 3~4)을 두고 `CombatSystem.draw`에서 알파 감쇠로 그린다. 레이저는 이미 빔.
  → 기본값 4. `VisualEffects.pushTrail`이 reduced-motion과 `projectileTrailLen=0`을 함께 처리한다.
  레이저는 투사체가 아니라 빔이라 이 경로에 오지 않는다.
- [x] **감전**: `EnemySystem.draw`에서 `statuses.shocked`가 있으면 스프라이트 위에 0.3초 주기 스파크 점 2~3개. **융해**: 주황 틴트(`globalCompositeOperation` 없이 반투명 원 덧칠로 충분). 글자 표시는 유지.
  → 스파크 3개, 위상에 `e.id`를 섞어 적마다 다르게 돈다. 감전 점멸은 reduced-motion에서 끈다
  (융해 틴트는 움직임이 아니라 유지).

### A-7. 사운드 호출 지점 심기 (소리는 아직 없음)

- [x] `GameAudio.play(kind)`의 `kind`를 아래로 확장하고, 미정의 kind는 무음으로 통과시킨다: `hit_chain` `hit_explosion` `hit_scatter` `hit_laser` `kill` `kill_boss` `merge` `generate` `order_complete` `skill_use` `core_hit` `core_low` `wave_clear` `boss_alert` `clear` `defeat`.
  → `GameAudio.MELODIES` 표를 뒀다(19종 = 기존 3 + 신규 16). 값이 `null`이면 조용히 통과한다.
  **전에는 정의되지 않은 kind가 440Hz 비프로 떨어졌다** — 새 호출을 심으면 전부 같은 소리가 났을 것이다.
  소리는 로드맵 6단계에서 이 표만 채우면 되고 호출부는 손대지 않는다.
- [x] 각 연출 지점에서 호출만 추가. 로드맵 6단계에서 신스 또는 에셋을 채운다.
  → 미사일 4종 적중음은 `VisualEffects.HIT_SOUNDS` 표로 `enemyHit` 한 곳에서 갈린다.

### 하지 않는 것
- 피해 수치·판정·적 이동·공격 주기 변경.
- 새 아트 에셋. 캔버스 도형과 CSS만.
- 피해 숫자 캔버스 이관(3군).

### 완료 조건
- [x] 스테이지 5 이상 물량 WAVE에서 프레임이 눈에 띄게 떨어지지 않는다(파티클 상한 180 유지).
  → 적 54기 상황에서 3초 샘플링: 평균 16.67ms(60fps), p95 16.7ms, 최악 16.8ms. 파티클 최대 20/180.
  개발·출시 진입점 동일.
- [x] 근접·원거리 공격이 오기 전에 보이고, 핵이 맞는 게 보인다.
- [x] 주문서 완료 시 어느 영웅이 강해졌는지 보인다.
- [x] reduced-motion에서 새 움직임이 전부 꺼진다.
  → `prefers-reduced-motion: reduce`로 확인 — 예비동작 0, 트레일 미기록, 핵 압축 미발동,
  `#core-alert` 애니메이션 `none`(정적 표시로 남아 경고 자체는 보인다).
- [x] 밸런스 에디터에서 새 수치가 보인다.
  → 당시 자동 회귀 검사 4조합도 통과했고, 에디터에 "인플레이 연출" 섹션 9개를 추가했다(입력 423 → 432개).

---

## 4. 연출 세션 B · 로비·성장 도파민

**목적**: 성장 행동마다 "무엇이 얼마나 좋아졌는지"가 보이고, 큰 순간(승급·돌파·전설)이 작은 순간(레벨업)과 구분된다.
**시점**: 개발계획서 세션 4 뒤(원래 기준이던 세션 5는 취소됐다). 로비 레이아웃은 세로 하나로 확정됐으므로 두 번 일할 걱정 없이 지금 붙여도 된다.

### B-1. 수치 변화 공통 유틸

- [ ] `GameFeedback.countUp(el, from, to, {duration, format})` 하나. 숫자를 tween하고 끝에 짧은 글로우(`fx-gain` 재사용). `Intl.NumberFormat`은 세션 3의 i18n 헬퍼를 쓴다.
- [ ] 적용 대상: `CampaignView.wallet`의 골드·별불·행동력, 캐릭터·스킬 모달의 능력치, 결과 화면 골드, 마일스톤 수령.
- [ ] 값이 줄어들 때(레벨업 비용 지출)는 카운트다운 + 붉은 톤. 늘 때는 카운트업 + 금색.

### B-2. 레벨업 · 승급 · 패시브 해금 구분

- [ ] **레벨업**: 모달에 능력치 전→후를 나란히 표시하고 후 값을 countUp. 기존 `growth-burst` 유지.
- [ ] **승급**: 별 아이콘이 하나씩 채워지는 CSS(0.15초 간격), 초상 흰 플래시 1회, 등급색 링 확산. `growth-burst`보다 확실히 크게.
- [ ] **패시브 해금(3성)**: 승급 연출 뒤에 패시브 카드가 아래에서 올라와 이름·효과를 보여주고 확인 버튼. `STAR_PASSIVE_UNLOCK` 값에서 시점을 읽는다.
- [ ] 스킬 승급(액티브 효과 성장)도 같은 승급 연출을 쓴다.
- [ ] 사운드 호출 지점: `level_up` `star_up` `passive_unlock`.

### B-3. 결과 화면 순차 등장

- [ ] `Game.renderResultStats`를 단계형으로: 제목 → 통과 WAVE(countUp) → 골드(countUp) → 최고 기록 갱신이면 "NEW BEST" 스탬프(회전 살짝 + 스케일 팝) → 받을 수 있는 마일스톤 안내 줄.
- [ ] 스테이지 돌파 별불은 **여기서 미리 보여주고** 수령은 로비 마일스톤에서 한다. 자동 수령으로 바꿀지는 기획 결정(미결 항목)이라 이 세션에서는 안내만.
- [ ] 단계 간격은 참고 0.25초, 탭하면 전부 즉시 표시.
- [ ] 패배 화면은 순차 없이 간결하게. 도달 WAVE만 강조.

### B-4. 마일스톤

- [ ] 로비 탭 버튼과 홈의 마일스톤 진입점에 수령 가능 뱃지(점 또는 개수). `MilestoneSystem.claimable`을 `Campaign.render`에서 한 번 계산.
- [ ] 수령 시 해당 행이 접히며 헤더 재화가 countUp. 토스트는 유지.

### B-5. 소환 보강

- [ ] 카드가 뒤집힐 때 신규면 `NEW` 리본, 중복이면 카드 아래에 `조각 +N → 현재/필요` 진행바가 0.3초 채워진다. 값은 `ShardLedger`/인벤토리에서 읽는다.
- [ ] 요약 화면의 신규 수·조각 합계는 countUp.
- [ ] `.gacha-chest` 부유 애니메이션 복구 여부는 인철 결정(테마 교체 때 의도적으로 껐는지 불명 → 미결 항목).

### B-6. 로비 상태 변화

- [ ] **새 스테이지 해금**: 결과 화면에서 로비로 돌아올 때 `unlocked`가 늘었으면 스테이지 배너가 한 번 열리는 연출(스케일 0.96→1 + 글로우 0.8초) 후 `stageHeader` 갱신.
- [ ] **편성 변경**: 홈 파티 요약의 해당 슬롯이 `growth-burst`.
- [ ] **토스트 큐**: `GameFeedback.toast`를 최대 3개 스택으로. 각 2.2초, 새 메시지는 아래에 쌓이고 위부터 사라진다.

### 하지 않는 것
- 새 화면·새 기능·보상 수치 변경.
- 소환 확률·연출 시간 같은 기획 확정값 변경.

### 완료 조건
- [ ] 레벨업·승급·패시브 해금이 눈으로 구분된다.
- [ ] 재화가 바뀌는 모든 지점에서 countUp이 돈다.
- [ ] 결과 화면이 순차로 뜨고 탭으로 스킵된다.
- [ ] 세로·가로 둘 다에서 연출 위치가 맞는다.

---

## 5. 3군 · 질감 (로드맵 6단계와 함께)

- **사운드 전면**: `GameAudio`가 오실레이터 구조라 A-7·B의 kind별로 신스 프로토타입을 먼저 만들고(타격은 짧은 노이즈+피치 드롭, 머지는 상승 아르페지오 등) 나중에 에셋만 교체. 기본값 켜짐 여부는 이때 결정. CrazyGames는 첫 입력 전 자동재생이 막히므로 첫 탭에서 컨텍스트를 깨우는 처리가 필요하다(지금 `resume()` 호출은 있음).
- **피해 숫자 캔버스 이관**: 40기 물량전 × 미사일 4종에서 DOM 생성·삭제가 초당 수십 회. 큰 화면에서 캔버스가 커지면 더 부담. `spawnDamageStack`을 `VisualEffects` 큐로 옮기고 텍스트를 캔버스에 그린다. 색·CRIT·관통 표기는 유지.
- **스킬 시전 질감**: 시전 순간 화면 가장자리 스킬 색 플래시(0.2초), 광역 스킬은 카메라 1.02배 당김.
- **보스 처치 슬로우 강화**: 현재 0.16초 히트스톱에 0.4초 잔여 슬로우(배속 0.5)를 더해 결과 화면 전환과 잇는다.

---

## 6. 미결 항목 (연출)

- 스테이지 돌파 별불을 결과 화면에서 자동 수령으로 바꿀지 (지금은 로비 마일스톤 수령)
- 소환 상자 부유 애니메이션 복구 여부
- 사운드 기본값 켜짐 여부
- 근접 예비동작 길이 0.25초가 방어 스킬 반응 시간으로 충분한지 (실기기에서 확인)
