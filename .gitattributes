# Auto detect text files and perform LF normalization
* text=auto

# [2026-09-18 세션 1] 표시 자산은 바이너리다 — LF 정규화·diff 대상에서 뺀다.
*.webp binary

# [2026-09-18 세션 6] 웹폰트도 바이너리다 — 줄바꿈 정규화가 닿으면 파일이 깨진다.
*.woff2 binary
