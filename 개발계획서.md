# 슬래그마 · CrazyGames Basic Launch 개발계획서

작성일: 2026-09-18 · 확정: 인철
이 문서는 `기획서.md`의 "출시 로드맵" 8단계 중 **2단계(구조 리팩토링) · 5단계 일부(영어화) · 7단계(출시 빌드·플랫폼 연동)**를
실제 작업 세션 단위로 풀어 쓴 것이다. 기획 결정과 히스토리는 계속 `기획서.md`에 기록하고, 이 문서는 **작업 순서와 범위**만 다룬다.

## 이 문서를 쓰는 방법

- 세션마다 아래 순서대로 하나씩 진행한다. 한 세션이 한 PR이다.
- 각 세션의 "완료 조건"이 모두 만족돼야 다음 세션으로 넘어간다. 다 못 하면 그 세션을 이어서 한다.
- 각 세션의 "하지 않는 것"은 범위를 넘지 않기 위한 선이다. 그 작업이 필요해 보여도 이 문서의 해당 세션이나 미결 항목에 적어 두고 넘어간다.
- 세부 구현(파일 이름·함수 시그니처·CSS 값)은 여기 적힌 것을 **참고안**으로 삼되, 작업 중 더 나은 판단이 있으면 그쪽을 택하고 기획서의 "구현 기술 선택" 섹션에 이유를 남긴다. 이 문서의 목적은 빠뜨리지 않는 것이지 그대로 따르게 하는 것이 아니다.
- 완료한 항목은 체크박스를 채운다.
- **검증 순서**(인철 확인 2026-09-18): 한 세션이 끝나면 **먼저 머지한다**. 인철의 실기기 확인은 머지된
  `main`에서만 가능하므로, 머지 전에 실기기 결과를 기다리지 않는다. 헤드리스 검증(스모크·수치 확인)은
  머지 전에 끝내고, 실기기 확인은 머지 뒤에 받는다. 문제가 나오면 후속 커밋으로 고친다.

## 확정된 전제 (2026-09-18, 인철)

- **동결**: 이 계획이 끝날 때까지 밸런스 수치·콘텐츠(수호자·스킬·스테이지·마일스톤)·임시 전설 3명의 패시브는 건드리지 않는다. 실기기 플레이 검증은 인철이 별도로 한다.
- **스크립트 로딩**: classic `<script src>` 순서 로딩. 빌드 도구·번들러·ES 모듈 없음. 로컬에서 파일을 더블클릭해 열려야 한다.
- **개발 옵션**: 크리티컬 머지·골든 피스 `featureFlags`는 남긴다. 확률 기본값 0 유지. 나중에 쓴다.
- **언어**: 기본 언어 영어. 한국어 유지. 옵션 메뉴에서 언어 변경. 언어 파일 하나를 추가하면 새 언어가 붙는 구조.
- **지표**: ByteBrew로 게임 지표 추적. 게임 코드는 ByteBrew를 직접 호출하지 않고 분석 어댑터를 거친다.
- **플랫폼**: CrazyGames Basic Launch. 데스크톱과 모바일 둘 다 지원하되, **레이아웃은 세로 하나**다
  (2026-09-18 세션 4에서 확정). 데스크톱 16:9에서는 세로 열을 가운데 두고 좌우를 배경 아트·비네팅으로
  채운다. 별도의 가로 배치는 만들지 않는다.

## 전체 순서

| 세션 | 단계 | 내용 |
|---|---|---|
| 1 | 리팩토링 | 이미지 자산 분리 + 파일 분할 (동작 보존) |
| 2 | 리팩토링 | 플랫폼 어댑터 3종 + 개발/출시 진입점 분리 + 스모크 스크립트 + 이벤트 호출 지점 |
| 3 | 리팩토링 | 문자열 테이블화(한국어) + 언어 시스템 + 옵션 언어 변경 |
| 4 | 반응형 | 폰 가로 처리 (세로 레이아웃 고정) |
| ~~5~~ | — | 취소 — 세션 4로 통합 (2026-09-18) |
| 6 | 영어화 | 영어 테이블 + 기본 언어 영어 + 넘침 정리 |
| 7 | 런치 | ByteBrew 연동 → CrazyGames SDK 연동 → 출시 빌드 → QA |

세션 6은 세션 3만 끝나 있으면 세션 4보다 먼저 해도 된다. 나머지는 순서대로.

연출(타격감·로비 피드백)은 `연출개선안.md`에 별도로 있다. **연출 세션 A(인플레이)**는 세션 2 뒤, **연출 세션 B(로비·성장)**는 세션 4 뒤에 끼워 넣는다(원래 기준이던 세션 5는 취소됐다). 사운드·질감은 로드맵 6단계.

> 2026-09-18: **연출 세션 A 완료**(세션 2 뒤, 예정대로). 기획서 `v0918_4` 참고. 세션 A가 추가한
> 오버레이 세 개(`#core-alert` 저HP 테두리, `#boss-hp` 보스 HP바, `#fx-layer` 보드→전장 연출 계층)는
> 원래 세션 4·5의 가로 레이아웃에서 위치를 다시 볼 예정이었으나, **가로 레이아웃을 만들지 않기로
> 하면서(세션 4) 재배치할 것이 없어졌다.** 셋 다 세로 레이아웃 기준 그대로 둔다.

---

## 세션 1 · 이미지 자산 분리 + 파일 분할

**목적**: 5.9MB 단일 `index.html`을 자산·CSS·JS 파일로 나눈다. 동작은 바꾸지 않는다.

현재 `index.html` 안에는 이미 섹션 마커가 있다. 이 경계대로 자르면 된다.

- CSS: `base.css` `theme.css` `play.css` `battle.css` `finish.css`
- JS: `config.js` `art.js` `core.js` `heroes.js` `combat.js` `board.js` `tools.js` `game.js` `characters.js` `character-ui.js` `campaign-ui.js` `campaign.js`

### 할 일

- [x] `ASSET_URLS`의 base64 webp 9장을 `assets/` 폴더의 실제 `.webp` 파일로 추출하고 `ASSET_URLS`는 파일 경로를 가리키게 한다. (HTML이 약 5.5MB 줄어든다.)
  → **9장 중 6장만** 파일로 뺐다. `fighters_01~03`은 `getImageData` 색키 처리 때문에 `file://`에서 캔버스가
  오염돼 읽을 수 없어 base64로 남겼다(아래 미결 항목). `index.html`은 5.9MB → 11.8KB, `js/assets.js`가 1.3MB다.
- [x] 마커 기준으로 CSS를 `css/` 폴더 파일들로, JS를 `js/` 폴더 파일들로 옮긴다. 마커 순서가 곧 로딩 순서다. `<link>`·`<script src>`를 그 순서대로 나열한다.
  → CSS 6개(계획의 5개 + `overrides.css`), JS 13개(계획의 12개 + `assets.js`). 이유는 기획서 v0918_1에 있다.
- [x] 파일을 나누되 **코드 내용은 바꾸지 않는다**. 전역 참조 방식도 그대로 둔다(모든 파일이 한 전역 스코프를 공유하므로 classic 로딩에서 그대로 동작한다).
  → 이어붙여 원본과 바이트 비교로 확인했다(CSS 126,960 · JS 258,322바이트 동일). 예외 한 곳: `GameArt.init`이
  CSS 커스텀 속성에 넣는 `url()`을 문서 기준 절대 URL로 바꿨다 — CSS가 `css/`로 내려가며 상대 경로가
  `css/assets/...`로 풀려 404가 났다.
- [x] `balance-editor.html`은 `index.html`을 iframe으로 띄우고 `CONFIG`에 접근한다. 분할 후에도 그대로 동작하는지 확인한다.
  → 고치지 않고 동작한다. 입력 423개 생성, `player.atkSpeed` 1→8 적용이 게임 `CONFIG`에 반영됨.
- [x] `.gitattributes`에 webp를 binary로 표시한다.

### 하지 않는 것

- 함수·객체 이름 변경, 죽은 코드 제거, 로직 정리. 전부 다음 세션 이후로.
- `기획서.md` 히스토리 수정. 구현 기술 선택 섹션에 한 줄만 남긴다.

### 완료 조건

- [x] 브라우저에서 로컬 파일로 열어 로비 → 전투 입장 → 한 WAVE 진행 → 중도 종료 → 결과까지 콘솔 에러 없이 된다.
  → 헤드리스 크로미움 430×900, `file://`·`http://` 양쪽. WAVE 3까지 진행 후 종료·결과·로비 복귀. 에러 0.
- [x] 저장 데이터(`slagma.campaign.v4`)가 분할 전과 그대로 읽힌다.
  → 분할 전 빌드로 만든 저장을 분할 후 빌드에 넣어 확인. 16개 키·값 모두 불변.
- [x] `balance-editor.html`이 열리고 값 변경이 게임에 반영된다.
- [x] 이미지 9장이 전부 보인다(로비 배경, 초상, 적 스프라이트 등).
  → 9장 모두 로드되고 fighters 색키 투명도가 `file://`·`http://`에서 동일하다(54.8 / 54.7 / 55.5%).

---

## 세션 2 · 플랫폼 어댑터 + 진입점 분리 + 스모크 스크립트

**목적**: 저장·분석·플랫폼을 얇은 어댑터 뒤로 보내고, 개발용 기능을 출시 진입점에서 뺄 수 있게 하고, 이후 세션의 회귀를 잡을 자동 검사를 하나 만든다.

### 2-1. 어댑터 3종 (`js/platform.js` 하나에 두는 것으로 충분)

세 어댑터 모두 **인터페이스만 정하고 지금은 가장 단순한 구현체**를 꽂는다. 게임 코드가 `localStorage`·SDK·분석 라이브러리를 직접 부르는 곳을 없애는 것이 목표다.

- [x] **저장 어댑터** `Storage`: `load(key)` `save(key, value)` `remove(key)`. 구현체는 지금의 localStorage 그대로. `CampaignStore`와 `GameAudio`(효과음 설정)가 이걸 쓰게 바꾼다. 나중에 CrazyGames 저장이나 다른 저장소로 바꿀 때 이 파일만 고친다.
  → 이름은 **`SaveStorage`**로 뒀다. `Storage`는 브라우저 내장 인터페이스 이름(localStorage의 프로토타입)이라
  최상위 `const Storage`가 그 전역을 가린다. 구현체는 예외를 삼키지 않고 그대로 올린다 — `CampaignStore`가
  try/catch로 저장 실패를 사용자 문구로 바꾸고 있어서, 여기서 null을 돌려주면 저장이 막힌 브라우저가 오류 대신
  새 게임으로 조용히 시작한다.
- [x] **분석 어댑터** `Analytics`: `track(name, params)` `progression(status, stage, params)` `setUser(key, value)`. 구현체는 두 개 — 개발용은 콘솔 출력, 출시용은 세션 7까지 no-op. `status`는 `start` `complete` `fail` 세 값으로 고정한다(ByteBrew의 progression 이벤트가 이 형태다).
  → 개발용 구현체는 셋 이외의 `status`를 받으면 콘솔에 경고한다.
- [x] **플랫폼 어댑터** `Platform`: `init()` `loadingStart()` `loadingStop()` `gameplayStart()` `gameplayStop()` `languageHint()`. 구현체는 no-op, `languageHint()`는 `null` 반환. 세션 7에서 CrazyGames 구현체로 바꾼다.
  → 호출 지점도 같이 심었다: `init()`은 DOMContentLoaded, `gameplayStart()`는 전투 입장(`Campaign.enter`),
  `gameplayStop()`은 결과 화면 진입(`Game.finishRun`). `loadingStart/Stop`은 로딩 구간이 아직 없어 비워 뒀다
  (자산이 붙기 전 화면이 비는 구간이 생기면 그때 넣는다).
- [x] 어댑터 선택은 진입점(HTML)에서 한다. 게임 코드는 전역 `Storage` `Analytics` `Platform`만 본다.
  → 진입점이 세우는 `window.SLAGMA_DEV`로 기본값이 갈린다(개발=콘솔, 출시=no-op). 세션 7에서 실제 SDK를
  꽂을 때는 진입점에서 `installPlatform({analytics, platform})`을 부른다.

### 2-2. 개발/출시 진입점 분리

- [x] `index.html`은 **출시 진입점**이 된다. 밸런스 에디터 연동·데이터 초기화 버튼·`#action-log` 디버그 표시·개발용 콘솔 로그는 여기서 빠진다.
  → `#reset-data-btn` 마크업을 뺐고 `js/dev.js`가 만든다. `#action-log`는 2026-09-16에 이미 마크업에서 빠져
  있었고 되살리지 않았다 — `#screen-game`이 3행 그리드라 자식을 하나 더 붙이면 전투 화면 배치가 달라져서
  개발 진입점으로 한 플레이테스트가 출시 빌드를 대변하지 못한다. 마지막 문구는 `logAction.last`로 읽는다.
  밸런스 에디터 연동은 원래 `index.html`에 없다(에디터가 iframe에 다리 스크립트를 심는 방식).
- [x] `dev.html`을 **개발 진입점**으로 만든다. `index.html`과 같은 파일들을 로드하고 추가로 개발 기능을 켠다. 방식은 전역 플래그(예: `window.SLAGMA_DEV = true`)를 스크립트 로딩 전에 세팅하고, 게임 코드가 그 플래그로 개발 UI를 켜고 끄는 정도면 된다. 파일을 두 벌 관리하지 않는다.
  → `dev.html`은 **`tools/build-dev.mjs`가 `index.html`에서 생성**한다. 진입점이 둘이면 `<body>` 마크업
  107줄이 두 벌이 되고 한쪽만 고치는 사고가 나므로, 마크업 원본은 `index.html` 하나로 두고 기계적으로 만든다.
  생성물은 커밋하므로 게임을 열 때 node가 필요 없다. `index.html`과의 차이는 네 줄뿐이다 — `<title>` 표시,
  `window.SLAGMA_DEV=true`, `js/dev.js`. 낡았는지는 `--check`로 보고 스모크가 먼저 돌린다.
  (2026-09-18: fighters 아틀라스에 알파가 들어와 런타임 색키를 걷어내면서 네 번째였던 `js/assets-dev.js`를
  없앴다 — 두 진입점이 같은 자산 파일을 쓴다.)
- [x] `balance-editor.html`은 iframe 대상을 `dev.html`로 바꾼다.
  → 함께 낡어 있던 주석도 고쳤다(`DEFAULT_CONFIG`의 위치가 `index.html` → `js/config.js`).
- [x] 데이터 초기화는 개발 진입점에만 둔다. (기획서 "데이터 초기화" 항목의 기능 자체는 유지.)
  → `js/dev.js`가 버튼을 만들고 `campaign.js`는 없으면 건너뛴다.

### 2-3. 이벤트 호출 지점 심기

`Analytics`를 부르는 자리를 게임 코드에 지금 넣는다. 실제 전송은 세션 7에서 켜진다. 이벤트 목록은 아래 표를 기준으로 하되, 넣다가 명백히 빠진 것이 보이면 추가하고 표를 갱신한다.

| 이벤트 | 호출 시점 | 파라미터 |
|---|---|---|
| `session_start` | 페이지 첫 로드 | `lang`, `orientation`(portrait/landscape), `isNew`(저장 없음 여부) |
| `tutorial_step` | 튜토리얼 단계 통과 또는 건너뜀 | `step`, `skipped` |
| progression `start` | 전투 입장 | `stage` |
| progression `complete` | 최종보스 처치 | `stage`, `wave`, `durationSec` |
| progression `fail` | 패배 또는 중도 종료 | `stage`, `wave`, `durationSec`, `reason`(defeat/quit) |
| `stage_clear_first` | 새 최고 스테이지 클리어 | `stage` |
| `gacha` | 소환 1회 | `count`(누적 횟수) |
| `level_up` | 수호자·스킬 레벨업 | `kind`(character/skill), `id`, `level` |
| `star_up` | 승급 | `kind`, `id`, `star` |
| `skill_used` | 전투 중 스킬 사용 | `skill`, `wave` |
| `milestone_claim` | 마일스톤 수령 | `id`, `tier` |
| `language_change` | 옵션에서 언어 변경 | `from`, `to` |

- [x] 위 이벤트를 각 시점에 `Analytics.track` / `Analytics.progression`으로 호출한다. 파라미터 값은 숫자와 짧은 문자열만 쓴다.
  → `language_change`는 옵션 언어 UI가 없으므로 세션 3에서 넣는다. 나머지 11종 완료.
  `level_up`·`star_up`은 `GrowthRules.transact` 한 곳에서 부른다 — 캐릭터·스킬 × 레벨업·승급 네 조합이
  모두 그 함수를 지나므로 호출부를 네 군데 두지 않았다(`kind`·`id`를 옵션으로 넘긴다).
  progression `fail`의 `reason`은 `Game.onDefeat(immediate)`로 가른다 — `immediate`가 사용자가 누른
  중도 종료 경로(`RunHost.defeat`)다.
- [x] `session_start`의 `orientation`은 `innerWidth > innerHeight`로 판정한다.
  → **2026-09-18 확정**: 세션 4에서 레이아웃 판정 함수를 만들지 않기로 했으므로(레이아웃이 세로 하나뿐)
  이 임시 판정이 최종이다. 지표용 값이라 이걸로 충분하다. `isNew`는 `Campaign.init()`이 새 진행을 만들기 전에 저장 유무를 본다.

### 2-4. 스모크 스크립트

- [x] `tools/smoke.mjs` 하나. Playwright로 `dev.html`을 열어 콘솔 에러·페이지 에러를 수집하고, 로비 → 전투 입장 → 일정 시간(예: 20초) 진행 → 중도 종료 → 결과 화면까지 클릭으로 진행한다. 에러가 하나라도 있으면 실패.
  → 콘솔 경고·`requestfailed`·4xx·저장 기록 여부도 같이 본다. 자체 정적 서버를 띄운다 — `file://`은 응답
  코드가 없어 4xx를 볼 수 없다(게임은 `file://`에서도 돌지만 검사는 http로 한다).
- [x] 세로(예: 430×900)와 가로(예: 1280×720) 두 뷰포트로 각각 한 번씩 돈다. 가로에서도 세로 레이아웃이 그대로 나오는 것이 정상이므로(세션 4 확정) 에러 여부만 본다.
  → `--entry both`로 `dev.html`·`index.html` 양쪽 × 두 뷰포트 = 4조합을 돈다.
- [x] 실행 방법을 이 문서 맨 아래 "도구" 절에 적는다. Playwright 설치는 `package.json` 없이 `npx playwright`로 충분하면 그렇게 하고, 필요하면 최소 `package.json`을 둔다. 게임 자체에는 npm 의존성이 생기지 않는다.
  → 최소 `package.json`을 뒀다(`private`, devDependency 하나). 게임은 이 파일도 `node_modules`도 필요하지
  않다. 스모크는 저장소 설치본이 없으면 전역 설치를 찾는다 — ESM은 `NODE_PATH`를 보지 않아 `npm root -g`로
  경로를 만든다.

### 하지 않는 것

- 문자열 분리(세션 3). 어댑터에 들어가는 메시지도 아직 한글 그대로 둔다.
- ByteBrew·CrazyGames SDK 실제 연결(세션 7).
- 죽은 코드 정리는 어댑터로 대체되며 자연히 사라지는 것만.

### 완료 조건

- [x] `index.html`과 `dev.html` 둘 다 열리고, `index.html`에는 에디터·초기화·디버그 표시가 없다.
  → `file://`·`http://` 양쪽에서 확인. `index.html`은 `SLAGMA_DEV=false` · `Analytics=Noop` · 초기화 버튼 없음,
  `dev.html`은 `true` · `Console` · 버튼 있음. `index.html`에 개발 요소 마크업 0건.
- [x] 게임 코드에 `localStorage` 직접 호출이 남아 있지 않다(`platform.js` 제외).
- [x] `dev.html`에서 한 판 돌리면 콘솔에 이벤트 로그가 표의 순서대로 찍힌다.
  → `session_start` → progression `start` → `tutorial_step` ×2 → progression `fail`(reason quit) 순으로 찍힌다.
  `index.html`에서는 아무것도 찍히지 않는다.
- [x] 스모크 스크립트가 세로·가로 둘 다 통과한다.
  → `--entry both`로 4조합 전부 통과. 첫 판에는 가로 두 조합이 ⚙ 메뉴에 닿지 못해 실패했다 — 그게 아래
  도구 메뉴 버그였고, 고친 뒤로는 네 조합 모두 ⚙ 메뉴를 거치는 실제 UI 경로로 런을 끝낸다.

---

## 세션 3 · 문자열 테이블화 + 언어 시스템

**목적**: 코드 안의 모든 사용자 노출 문자열을 언어 파일로 빼고, 언어를 파일 추가만으로 늘릴 수 있게 한다. 이 세션이 끝나도 화면은 지금과 완전히 같아야 한다(한국어만 있으므로).

현재 규모: JS 안 한글 리터럴 약 560개, HTML·CSS 안 약 180개. 한 세션에 다 못 하면 화면 단위로 끊어서 두 세션으로 한다. 끊는 순서 참고안: 시작·로비·옵션 → 전투 HUD·주문서·스킬 → 결과·마일스톤·소환 → 설명서·튜토리얼 → 데이터(수호자·스킬·마일스톤 이름과 설명).

### 3-1. 구조

- [ ] `js/i18n.js`: 언어 레지스트리와 `t()`. `I18N.register(code, table)`, `I18N.setLanguage(code)`, `I18N.languages()`(등록된 언어 목록), `t(key, params)`.
- [ ] `i18n/ko.js`, (세션 6에서) `i18n/en.js`. 각 파일은 `I18N.register('ko', {...})` 한 번만 부른다. 새 언어 = 파일 하나 + `<script src>` 한 줄.
- [ ] 키는 점 표기 계층(예: `lobby.stamina`, `skill.spear.name`, `manual.section.flow.title`). 키 이름은 영어, 의미 기준으로 짓는다. 화면 이름을 접두어로 쓰되 여러 화면이 공유하는 문자열은 `common.` 아래에 둔다.
- [ ] 자리표시자는 `{name}` 형식. `t('battle.wave', {n: 5})`. CONFIG 수치가 들어가는 설명서·튜토리얼 문장은 전부 자리표시자로 만든다. 수치를 문자열에 직접 박지 않는다.
- [ ] 없는 키는 영어 테이블로 폴백하고, 그것도 없으면 키 자체를 반환한다. 개발 진입점(`SLAGMA_DEV`)에서는 빠진 키를 콘솔에 한 번씩 경고한다.
- [ ] 숫자는 `Intl.NumberFormat(현재 언어)`로 포맷하는 헬퍼 하나를 `i18n.js`에 둔다. 기존 `formatDamage` 같은 함수가 그것을 쓰게 바꾼다.

### 3-1-b. 한글이 아닌 사용자 노출 문자열 (놓치기 쉬움)

이 세션의 완료 조건은 "한글 리터럴이 없다"인데, **영문으로 화면에 나가는 문자열은 그 조건을 통과해
버린다**. 아래는 2026-09-18 기준으로 실제로 있는 것들이다. 테이블화 대상이며 세션 6에서 번역도 필요하다
(영어 기본이라 값은 같아 보여도 키는 있어야 한다 — 제3 언어가 붙을 때 드러난다).

- [ ] `#wave-label` — `WAVE {n} / {total}` (`js/combat.js` `startWave`)
- [ ] WAVE 배지 캡션 — `WAVE`, 클리어 시 `CLEAR` (`js/combat.js` `playWaveClearBeat`, 연출 세션 A에서 추가)
- [ ] 같은 캡션의 마크업 초기값 — `index.html`의 `.wave-type-caption`
- [ ] 피해 스택의 `CRIT ` 접두 (`js/core.js` `spawnDamageStack`)
- [ ] 위 목록은 `grep -rn "textContent\s*=\s*'[A-Za-z]" js/` 로 다시 훑어 확인한다. 작업 중 새로 생긴 것이
  있으면 여기에 추가한다.

### 3-2. 데이터 안의 문자열

- [ ] 수호자 이름·패시브 이름, 스킬 이름·설명, 마일스톤 이름·설명, 미사일·적 타입 이름, 희귀도·종족·정체성 이름은 **이름도 키로** 뺀다(인철 확정 2026-09-18: 고유명사도 번역 대상). 데이터 객체는 `nameKey` 같은 키만 갖고 표시 시점에 `t()`를 부른다.
- [ ] 저장 데이터에는 이름이 아니라 ID만 들어가는지 확인한다. 이름이 들어가 있으면 마이그레이션이 필요하므로 이 세션에서 ID로 바꾸고 `CampaignStore.migrate`에 보정을 추가한다.

### 3-3. 언어 선택

- [ ] 언어 설정은 저장 데이터와 **별도 키**(예: `slagma.lang`)에 `Storage`로 저장한다. 데이터 초기화에 영향받지 않는다.
- [ ] 초기 언어 결정 순서: 저장된 값 → `Platform.languageHint()` → `navigator.language` 앞 두 글자가 등록된 언어면 그것 → 기본값. 기본값은 이 세션에서는 `ko`, 세션 6에서 `en`으로 바꾼다.
- [ ] 옵션 UI에 언어 선택을 넣는다. 현재 상단 우측 ❓·⚙(효과음) 버튼 자리에 옵션 팝업을 하나 두고 효과음·언어를 함께 넣는 정도가 적당하다. 목록은 `I18N.languages()`에서 자동 생성하고 각 언어는 자기 이름을 자기 언어로 표시한다(`한국어`, `English`).
- [ ] 언어를 바꾸면 현재 화면을 다시 그린다. 전투 중 변경은 막아도 된다(로비·시작·결과에서만 허용).
- [ ] `<html lang>`을 현재 언어로 바꾼다. 폰트는 이 세션에서는 그대로 두고 세션 6에서 정리한다.
- [ ] `Analytics.track('language_change', ...)` 호출.

### 하지 않는 것

- 영어 번역 작성(세션 6).
- 밸런스 에디터의 한글은 개발 도구이므로 번역하지 않는다.
- `기획서.md`·이 문서는 한국어 그대로.

### 완료 조건

- [ ] 게임 코드(`js/`)와 `index.html`·`dev.html`에 사용자에게 보이는 한글 리터럴이 없다. (주석은 제외.)
- [ ] **영문으로 화면에 나가는 문자열도 없다** — 위 3-1-b 목록. 한글만 찾으면 이것들이 남는다.
- [ ] 화면이 세션 전과 동일하다. 설명서·튜토리얼의 수치가 CONFIG와 여전히 일치한다.
- [ ] 옵션에서 언어 목록에 `한국어` 하나가 보이고 선택이 저장된다.
- [ ] 스모크 통과. 개발 진입점 콘솔에 빠진 키 경고가 없다.

---

## 세션 4 · 폰 가로 처리 (세로 레이아웃 고정)

**목적**: 폰을 가로로 눕혔을 때 깨지는 것만 막는다. 레이아웃은 세로 하나로 유지한다.

> **2026-09-18 방향 전환 (인철 확정).** 원래 세션 4·5는 "데스크톱 16:9 가로 레이아웃"을 만드는
> 내용이었다. 실제 브라우저로 측정해 보니 **넓은 화면은 이미 정상 동작한다**(아래 측정 결과).
> 가로 레이아웃을 새로 만들면 없던 문제를 만들고, 남은 세션 6·7의 검증 대상이 두 배가 된다.
> 그래서 가로 레이아웃은 만들지 않고, 실제로 깨지는 폰 가로 하나만 처리한다. 세션 5는 취소한다.

### 측정 결과 (2026-09-18 · Playwright + Chromium · `index.html` 로비→전투 진입)

| 뷰포트 | `#combat-canvas` 실측 | 판정 |
|---|---|---|
| 430×900 (폰 세로) | 430×554 | 정상 |
| 1280×720 (노트북) | 428×396 | **정상** |
| 1920×1080 (데스크톱) | 앱 열 480×1080 | **정상** |
| 900×430 (폰 가로) | **162×150** | 깨짐 |

네 조합 모두 페이지 에러 0건.

**넓은 화면이 이미 되는 이유**

- `js/game.js`의 `layoutField()`가 `CONFIG.field.maxAnisotropy`(1.18)로 세로·가로 배율 차를 제한한다.
  컨테이너가 어떤 모양이든 한계를 넘으면 캔버스를 줄여 여백을 만든다 — 레터박스는 이미 구현돼 있다.
- 좌우 여백도 이미 채워져 있다. `css/theme.css`의 `body::before`가 `forge_lobby.webp`를 화면 전체에
  깔고 어둡게 덮고, `#app`의 `box-shadow:0 0 100px var(--bg),0 0 0 1px var(--primary)`가 비네팅과
  테두리를 만든다. `css/play.css`의 `@media(min-width:700px)`가 양옆 보더를 더한다.
- 도구 버튼도 앱 열을 벗어나지 않는다. 실측 `#tools-toggle` x=829(열 400~880, 폭 1280),
  x=1149(열 720~1200, 폭 1920). 세션 2에서 "넓은 화면 배치는 세션 4·5에서 본다"고 남겨 둔 항목은
  실제로는 문제가 없었다.
- 머지 드래그는 `js/board.js`가 `pointerdown`·`pointermove`를 쓰므로 마우스에서 이미 동작한다.

**폰 가로는 "가만히 둬도" 안 되는 이유**

지금 코드에 가로 대응 로직이 아예 없다. 그러므로 뺄 "반응"도 없다. 폰을 눕히면 뷰포트가 물리적으로
430px 높이가 되고, 900px 높이를 전제로 만든 세로 레이아웃이 그 안에 우그러든다. 위 표의 162×150이
**아무것도 하지 않은 상태**의 결과다.

방향 잠금 API(`screen.orientation.lock('portrait')`)는 대안이 못 된다 — iOS Safari는 미지원이고
Chrome Android는 전체화면에서만 동작한다. CSS `transform:rotate(90deg)`로 화면을 통째로 돌리는
방법은 `position:fixed` 컨테이닝 블록과 `getBoundingClientRect()` 계산(`layoutField`·`combatLayerPoint`),
포인터 드래그 좌표를 한꺼번에 틀어놓는다. 세 방법 중 오버레이가 가장 싸고 안전하다.

### 할 일

- [x] 폰 가로에서 회전 안내 오버레이를 띄운다. 판정은 CSS 하나로 한다 —
      `@media (max-height:500px) and (orientation:landscape) and (pointer:coarse)`.
      `pointer:coarse`가 터치 기기만 걸러 주므로 짧은 데스크톱 창은 영향을 받지 않는다.
  → `#rotate-notice`(`index.html`) + `css/overrides.css` 맨 아래 블록. JS는 한 줄도 쓰지 않았다.
  마크업은 **`#app` 바깥**에 뒀다 — `#app`이 `position:fixed` 자식의 컨테이닝 블록이라 안에 두면
  앱 열만 덮는다(`#manual-panel`이 그 경우다). 실측 4종: 900×430+터치 → 뜸, 1280×720 → 안 뜸,
  **1280×400(짧은 데스크톱 창) → 안 뜸**, 430×900 → 안 뜸.
- [x] 안내 문구는 세션 3의 문자열 테이블에 키로 넣는다(세션 6에서 영문이 필요하다).
  → 세션 3이 아직 시작 전이라 `i18n/`이 없다. 지금은 `index.html`의 한글 리터럴로 두고, 세션 3의
  완료 조건("`index.html`에 사용자 노출 한글 리터럴이 없다")이 이것도 함께 걷어가게 했다.
  `dev.html`은 생성물이므로 `index.html`만 고치고 `node tools/build-dev.mjs`를 돌리면 된다.
- [x] 오버레이가 떠 있는 동안 런이 끊기지 않는지 확인한다. 게임 루프는 멈추지 않고 화면만 덮으며,
      세로로 되돌리면 진행 중이던 전투가 그대로 이어져야 한다.
  → 전투 중 430×900 → 900×430 → 430×900으로 돌려 확인했다. 화면 `screen-game` 유지, WAVE·HP 그대로,
  런 타이머 00:03 → 00:04 → 00:06으로 **계속 흘렀다**(루프가 멈추지 않는다). 복귀 후 캔버스는
  회전 전과 같은 430×554. 페이지 에러 0건.
- [x] 스모크 뷰포트에 900×430을 추가한다. 그 조합에서는 오버레이가 떠 있는 것이 정상이다.
  → `rotateOnly` 플래그를 둬서 플레이스루 대신 안내가 보이는지만 본다. `hasTouch: true`가 있어야
  `pointer:coarse`가 맞는다(기본 컨텍스트는 `pointer:fine`이라 안내가 뜨지 않는다). `isMobile`은
  쓰지 않았다 — meta viewport가 적용돼 크기가 900×430이 아니라 980×469로 바뀐다(실측).
  꼬리 출력(이벤트·note·problem·페이지 닫기)은 세 경로가 공유하도록 `report()`로 묶었다.

### 하지 않는 것

- **가로 레이아웃**. 전투·로비·시작·결과 어느 화면도 2열로 재배치하지 않는다.
- `Layout.mode()` 같은 레이아웃 판정 함수. 레이아웃이 하나뿐이므로 필요 없다.
- `#app`의 `max-width` 변경. 지금 값(`css/theme.css` 480px)이 최종이다.
- 캔버스 논리 해상도·히트박스·속도 등 CONFIG 판정값 변경.
- 새 아트.

### 완료 조건

- [x] 900×430에서 회전 안내가 뜨고, 세로로 되돌리면 진행 중이던 전투가 이어진다.
- [x] 1280×720 데스크톱 창에서는 안내가 뜨지 않는다. 1280×400(짧은 창)에서도 뜨지 않는다.
- [x] 430×900 세로와 1280×720·1920×1080이 세션 전과 동일하다.
- [ ] 스모크 통과. → **새로 넣은 폰 가로 조합은 `dev.html`·`index.html` 양쪽 PASS.** 다만 기존
  네 조합이 "중도 종료 → 결과" 단계에서 `#quit-run` 클릭 타임아웃으로 실패한다. 이 세션의 변경을
  전부 빼고(`git stash`) `origin/main` 그대로 돌려도 같은 단계·같은 메시지로 실패하므로 **이 세션과
  무관한 기존 문제**다. 아래 미결 항목으로 옮겼다. 그것이 해소되면 이 칸을 채운다.

---

## 세션 5 · ~~로비·시작·결과 화면 가로 레이아웃~~ (취소)

2026-09-18 취소(인철 확정). 세션 4의 측정대로 넓은 화면은 이미 정상 동작하므로 만들 것이 없다.
남아 있던 폰 가로 항목은 세션 4로 옮겼다. 세션 번호는 6·7의 상호 참조를 깨지 않기 위해 비워 둔다.

> 되살릴 근거가 생기면(예: CrazyGames 심사가 데스크톱 가로 배치를 요구) 이 세션을 복원한다.
> 그때 쓸 참고안(2열 로비, 수호자 `auto-fill` 6~8열, 전투 우측 UI 열)은 git 이력에 있다 —
> 이 문서의 2026-09-18 이전 버전.
---

## 세션 6 · 영어화

**목적**: 영어 테이블을 채우고 기본 언어를 영어로 바꾼다.

### 할 일

- [ ] `i18n/en.js` 작성. `ko.js`와 키가 1:1로 같아야 한다. 키 차이를 찾는 간단한 스크립트를 `tools/`에 두면 좋다(두 테이블의 키 집합 비교).
- [ ] 기본 언어를 `en`으로 바꾼다. 브라우저가 한국어면 `ko`로 열리는 것은 세션 3의 결정 순서가 처리한다.
- [ ] 수호자·스킬 이름 영문 표기는 이 세션에서 인철에게 확인받는다. 음차(Miren, Astel)로 갈지 의역으로 갈지. 확인 전까지는 음차로 채워 둔다.
- [ ] 영문이 한글보다 길어 넘치는 곳을 세로·가로 두 레이아웃에서 잡는다. 특히 스킬 버튼, 주문서 카드, 마일스톤 목록, 소환 요약, 튜토리얼 카드. 글자 크기를 줄이기보다 줄바꿈·말줄임을 먼저 쓴다.
- [ ] 폰트: 한국어 폰트(Gothic A1)는 `ko`일 때만 로드되게 한다. 숫자 폰트(Orbitron)는 공통.
- [ ] `<title>`·메타 설명도 언어에 따른다.

### 하지 않는 것

- 제3 언어 추가. 구조만 열어 둔다.
- 설명서 내용 재구성. 번역만.

### 완료 조건

- [ ] 두 테이블의 키가 일치한다.
- [ ] 영어로 세로·가로 한 판씩 돌려 넘침·잘림이 없다.
- [ ] 옵션에서 `English` ↔ `한국어` 전환이 로비·시작·결과에서 즉시 반영된다.
- [ ] 스모크 통과.

---

## 세션 7 · ByteBrew → CrazyGames SDK → 출시 빌드 → QA

**목적**: 어댑터에 실제 구현체를 꽂고 출시 빌드를 확정한다. 순서는 **ByteBrew 먼저**. 이벤트가 대시보드에 실제로 흐르는 것을 본 뒤 플랫폼을 붙이는 편이 디버깅이 쉽다.

### 7-1. ByteBrew

- [ ] ByteBrew 웹(HTML5) SDK를 `index.html`에서만 로드한다. `dev.html`은 콘솔 구현체 유지.
- [ ] `Analytics`의 출시 구현체를 작성한다. `track` → 커스텀 이벤트, `progression` → progression 이벤트, `setUser` → 사용자 속성. **정확한 SDK 함수명은 이 세션에서 ByteBrew 문서를 보고 확정한다.** 이 문서의 함수명을 믿지 말 것.
- [ ] 게임 ID·SDK 키는 `platform.js`가 아니라 `index.html`의 초기화 스크립트에 둔다. 저장소가 비공개라 코드에 직접 넣어도 된다(인철 확인 필요 — 미결 항목).
- [ ] SDK 로드 실패·차단(광고 차단기 등)에도 게임이 멈추지 않는다. 어댑터 안에서 try/catch로 삼킨다.
- [ ] 대시보드에서 `session_start`와 progression 이벤트가 보이는 것을 확인한다.

### 7-2. CrazyGames SDK

- [ ] SDK를 `index.html`에서 로드하고 `Platform` 출시 구현체를 작성한다. `init` → SDK 초기화, `loadingStart/Stop` → 로딩 구간 신호, `gameplayStart/Stop` → 전투 입장·종료, `languageHint` → SDK가 주는 사용자 언어.
- [ ] `gameplayStart`는 전투 입장, `gameplayStop`은 결과 화면 진입에서 부른다. 로비는 게임플레이 아님.
- [ ] 저장은 이번 런치에서 localStorage를 유지한다. CrazyGames 저장으로 바꾸는 것은 미결 항목에 둔다.
- [ ] SDK가 없는 환경(로컬 파일, 다른 호스트)에서도 게임이 동작한다. `Platform` 구현체가 SDK 부재를 감지하면 no-op으로 떨어진다.
- [ ] 광고·결제·로그인은 이번 런치 범위 밖. 기획서 "제외 범위"와 같다.

### 7-3. 출시 빌드 확정

- [ ] `index.html`에 개발 기능이 없는지 다시 확인(에디터·초기화·디버그 로그·콘솔 이벤트 출력·빠진 키 경고).
- [ ] CrazyGames 업로드 형태(zip 안에 `index.html`이 루트)를 만들 수 있도록 포함할 파일 목록을 정한다. 목록을 이 문서 "도구" 절에 적는다.
  - **포함**: `index.html` · `css/` 6개 · `js/` 중 게임 코드 14개 · `assets/` 9장
  - **제외**: `dev.html` · `js/dev.js` · `balance-editor.html` · `tools/` · `package.json` · `.gitignore` ·
    `기획서.md` · `개발계획서.md` · `연출개선안.md` · `.gitattributes` · `node_modules/`
  - 제외 목록은 파일이 늘 때 같이 갱신한다. 세션 2 이후 `js/dev.js`·`package.json`·`.gitignore`가 생겼다.
  - **파일 목록에만 의존하지 말고 검사 한 줄을 둔다.** 출시 빌드에 개발 전용 파일이 섞여도 게임은 정상
    동작하므로 증상이 없다(2026-09-18에 `js/assets-dev.js` 1.35MB가 그런 위험이었고, 그건 파일 자체를
    없애 해결했다). zip을 만든 뒤 `dev`·`tools`·`balance-editor`·`package.json`이 들어 있지 않은지 본다.
- [ ] 자산 경로가 전부 상대 경로인지 확인.

### 7-4. QA 체크리스트

- [ ] 데스크톱 1920×1080 · 1280×720 가로에서 로비·전투·결과·소환 한 바퀴
- [ ] 모바일 세로에서 한 바퀴
- [ ] 폰 가로(예: 900×430)에서 회전 안내가 뜨고, 세로로 되돌리면 전투가 이어진다
- [ ] 전투 중 새로고침 → 로비로 돌아오고 저장이 유지된다
- [ ] 탭 전환·백그라운드 복귀 후 전투가 이어진다(또는 일시정지 후 재개)
- [ ] 언어 선택이 새로고침 후 유지된다
- [ ] 저장 없는 첫 방문에서 튜토리얼이 뜨고 영어로 나온다
- [ ] 분석 이벤트가 대시보드에 찍힌다
- [ ] 스모크 통과

### 완료 조건

- [ ] QA 체크리스트 전부 통과. 기획서 로드맵 7단계 체크박스를 채운다.

---

## 미결 항목 (이 계획 범위)

작업 중 결정이 필요해진 것을 여기 쌓는다. 확정되면 해당 세션 항목에 반영하고 `기획서.md` 히스토리에도 기록한다.

- **스모크의 "중도 종료 → 결과" 단계가 `#quit-run` 클릭 타임아웃으로 실패한다** (2026-09-18 세션 4에서
  발견, 원인 미확인). `origin/main`에서도 동일하게 재현되므로 세션 4의 변경과는 무관하다. 확인된 것까지:
  전투 진입 직후 `#quit-run`은 `display:none`이고, ⚙를 누르면 `display:block · opacity:1 ·
  pointer-events:auto`로 정상 전환된다 — 버튼 자체는 살아 있으므로 다른 요소가 클릭을 가로채는 쪽이
  의심된다. 실제 플레이에서 종료가 되는지부터 확인이 필요하다(스모크만의 문제인지, 게임 버그인지).
- ByteBrew 게임 ID·SDK 키를 코드에 직접 넣을지 (저장소 비공개 여부 확인)
- CrazyGames 포털에 지원 방향을 portrait로 선언하면 회전 안내를 플랫폼이 자기 UI로 처리해 줄 수 있다.
  그렇다면 세션 4의 오버레이는 불필요해진다 → **세션 7에서 확인하고, 중복이면 오버레이를 걷어낸다.**
- 수호자·스킬 이름 영문 표기 방식 (음차 / 의역)
- CrazyGames 저장 API로 전환할지 (이번 런치는 localStorage 유지)
- 스모크 스크립트를 세션 1 끝에 당겨 만들지 (분할 직후가 가장 안전하나 세션 1이 길어짐) → 세션 1에서는 만들지
  않았다. 같은 내용의 임시 스크립트로 검증만 하고 `tools/smoke.mjs`는 계획대로 세션 2에서 만든다.
- **`fighters_01~03`을 파일로 뺄지** (세션 1 보류, 인철 판단 필요). CrazyGames 규칙 대조에서 확인된 사실
  (기획서 v0918_2) — 규칙 위반은 아니지만 출시 빌드에서는 순손해다. `js/assets.js`의 1,352KB base64가
  파서 블로킹 스크립트로 크리티컬 패스에 올라가고(초기 전송 4,765KB vs 4,426KB, DOMContentLoaded
  171ms vs 90ms), cross-origin iframe + https에서는 파일로도 색키가 똑같이 동작한다. 즉 인라인의 유일한
  이유는 `file://` 더블클릭 개발 편의다. 남은 선택지 세 개:
  - **(i) 현재 유지.** 모든 환경에서 동작이 같다. 출시 빌드에 1,352KB 파서 블로킹이 남는다.
  - **(ii) 9장 전부 파일 + `prepareFighterAtlas`에 try/catch 폴백.** 출시 빌드 최적. `file://` 더블클릭에서는
    픽셀을 못 읽어 영웅 뒤에 초록 배경이 보인다(게임은 정상 동작). 개발 화면이 나빠진다.
  - **(iii) 알파 webp를 한 번 구워 두기.** 브라우저 인코더로는 역효과임을 실측했다(4,461KB, 원본의 4배 —
    기획서 v0918_2). `cwebp -lossless` 같은 실제 인코더가 필요하고 이 환경에 없다. 또 "색키 원본 픽셀을
    다른 엔진에서 재사용하려고 남긴다"는 현재 주석과 충돌한다.
  - 세션 7 출시 빌드를 만들 때까지는 결정을 미뤄도 된다. (ii)를 고르면 개발/출시 진입점이 갈리는
    세션 2에서 같이 처리하는 게 자연스럽다.
- **localStorage가 cross-origin iframe에서 파티션된다** (세션 7 저장 항목과 함께 볼 것). CrazyGames 임베드
  구조를 모사해 확인해보니 읽기·쓰기는 정상 동작하지만, 브라우저의 third-party storage partitioning 때문에
  저장이 상위 사이트별 파티션에 묶인다. 계획서의 "저장은 이번 런치에서 localStorage를 유지한다"와 충돌하지는
  않지만, 이용자가 사이트 데이터를 지우거나 브라우저가 파티션을 정리하면 진행이 날아갈 수 있다. 세션 7에서
  CrazyGames 저장 API 전환을 볼 때 이 사실을 근거로 같이 판단한다.

## 이 계획에 넣지 않은 것 (로드맵의 다른 단계)

아래는 기획서 로드맵 3·4·6단계다. 이 계획이 끝난 뒤 별도로 잡는다. 이번 작업 중 손대지 않는다.

- 임시 전설 3명의 패시브 교체 (연쇄·폭발·산탄 고유 규칙 설계)
- 골드 소비처 추가, 일일 보상·업적·도감
- 효과음·배경음 (지금은 사인파 비프 1종, 기본 꺼짐)
- 아트·최종 UI
- 첫 이용자 1클릭 전투 진입, 튜토리얼 단순화

## 도구

게임 자체에는 npm 의존성이 없다 — `index.html`을 브라우저에서 열면 그대로 돌아간다. 아래는 개발 도구뿐이다.
`package.json`은 그 도구들만 쓰는 최소 매니페스트다.

**스모크 실행** (세션 2)

```
node tools/smoke.mjs                # dev.html · 세로 430×900 + 가로 1280×720
node tools/smoke.mjs --entry both   # dev.html·index.html 양쪽 = 4조합
node tools/smoke.mjs --entry index  # 출시 진입점만
node tools/smoke.mjs --headed       # 브라우저를 띄워서 본다
```

한 조합에 약 30초, 4조합에 2분 남짓 걸린다(WAVE를 실제로 진행한다). Playwright가 없으면
`npm install` 한 번. 진입점 생성물이 낡았는지 먼저 확인하고 낡았으면 바로 실패한다.

**개발 진입점 생성** (세션 2)

```
node tools/build-dev.mjs            # dev.html 생성
node tools/build-dev.mjs --check    # 낡았는지만 확인 (쓰지 않음)
```

`index.html`의 `<head>`나 `<body>`를 고쳤으면 이걸 다시 돌린다. 잊어도 스모크가 잡는다.
자산 파일 교체는 이제 이 스크립트와 무관하다(2026-09-18에 `js/assets-dev.js`를 없앴다).

**자산 추출** (세션 2, 일회성)

```
node tools/extract-assets.mjs       # js/assets.js의 base64 항목을 assets/*.webp로
```

`ASSET_URLS`에 base64가 남아 있지 않으면 아무것도 하지 않는다.

- 언어 키 비교: (세션 6에서 기록)
- 출시 빌드 포함 파일: (세션 7에서 기록)
