# 슬래그마 · CrazyGames Basic Launch 개발계획서

작성일: 2026-09-18 · 확정: 인철
이 문서는 `기획서.md`의 "출시 로드맵" 8단계 중 **2단계(구조 리팩토링) · 5단계 일부(영어화) · 7단계(출시 빌드·플랫폼 연동)**를
실제 작업 세션 단위로 풀어 쓴 것이다. 기획 결정과 히스토리는 계속 `기획서.md`에 기록하고, 이 문서는 **작업 순서와 범위**만 다룬다.

## 이 문서를 쓰는 방법

- 세션마다 아래 순서대로 하나씩 진행한다. 한 세션이 한 PR이다.
- 각 세션의 "완료 조건"이 모두 만족돼야 다음 세션으로 넘어간다. 다 못 하면 그 세션을 이어서 한다.
- 각 세션의 "하지 않는 것"은 범위를 넘지 않기 위한 선이다. 그 작업이 필요해 보여도 이 문서의 해당 세션이나 미결 항목에 적어 두고 넘어간다.
- 세부 구현(파일 이름·함수 시그니처·CSS 값)은 여기 적힌 것을 **참고안**으로 삼되, 작업 중 더 나은 판단이 있으면 그쪽을 택하고 기획서의 "구현 기술 선택" 섹션에 이유를 남긴다. 이 문서의 목적은 빠뜨리지 않는 것이지 그대로 따르게 하는 것이 아니다.
- 완료한 항목은 체크박스를 채운다.

## 확정된 전제 (2026-09-18, 인철)

- **동결**: 이 계획이 끝날 때까지 밸런스 수치·콘텐츠(수호자·스킬·스테이지·마일스톤)·임시 전설 3명의 패시브는 건드리지 않는다. 실기기 플레이 검증은 인철이 별도로 한다.
- **스크립트 로딩**: classic `<script src>` 순서 로딩. 빌드 도구·번들러·ES 모듈 없음. 로컬에서 파일을 더블클릭해 열려야 한다.
- **개발 옵션**: 크리티컬 머지·골든 피스 `featureFlags`는 남긴다. 확률 기본값 0 유지. 나중에 쓴다.
- **언어**: 기본 언어 영어. 한국어 유지. 옵션 메뉴에서 언어 변경. 언어 파일 하나를 추가하면 새 언어가 붙는 구조.
- **지표**: ByteBrew로 게임 지표 추적. 게임 코드는 ByteBrew를 직접 호출하지 않고 분석 어댑터를 거친다.
- **플랫폼**: CrazyGames Basic Launch. 데스크톱 16:9 가로와 모바일 세로 둘 다 지원.

## 전체 순서

| 세션 | 단계 | 내용 |
|---|---|---|
| 1 | 리팩토링 | 이미지 자산 분리 + 파일 분할 (동작 보존) |
| 2 | 리팩토링 | 플랫폼 어댑터 3종 + 개발/출시 진입점 분리 + 스모크 스크립트 + 이벤트 호출 지점 |
| 3 | 리팩토링 | 문자열 테이블화(한국어) + 언어 시스템 + 옵션 언어 변경 |
| 4 | 반응형 | 전투 화면 가로 레이아웃 |
| 5 | 반응형 | 로비·시작·결과 화면 가로 레이아웃 + 폰 가로 확인 |
| 6 | 영어화 | 영어 테이블 + 기본 언어 영어 + 넘침 정리 |
| 7 | 런치 | ByteBrew 연동 → CrazyGames SDK 연동 → 출시 빌드 → QA |

세션 4·5는 순서를 바꿔도 되고, 세션 6은 세션 3만 끝나 있으면 4·5보다 먼저 해도 된다. 나머지는 순서대로.

연출(타격감·로비 피드백)은 `연출개선안.md`에 별도로 있다. **연출 세션 A(인플레이)**는 세션 2 뒤, **연출 세션 B(로비·성장)**는 세션 5 뒤에 끼워 넣는다. 사운드·질감은 로드맵 6단계.

---

## 세션 1 · 이미지 자산 분리 + 파일 분할

**목적**: 5.9MB 단일 `index.html`을 자산·CSS·JS 파일로 나눈다. 동작은 바꾸지 않는다.

현재 `index.html` 안에는 이미 섹션 마커가 있다. 이 경계대로 자르면 된다.

- CSS: `base.css` `theme.css` `play.css` `battle.css` `finish.css`
- JS: `config.js` `art.js` `core.js` `heroes.js` `combat.js` `board.js` `tools.js` `game.js` `characters.js` `character-ui.js` `campaign-ui.js` `campaign.js`

### 할 일

- [x] `ASSET_URLS`의 base64 webp 9장을 `assets/` 폴더의 실제 `.webp` 파일로 추출하고 `ASSET_URLS`는 파일 경로를 가리키게 한다. (HTML이 약 5.5MB 줄어든다.)
  → **9장 중 6장만** 파일로 뺐다. `fighters_01~03`은 `getImageData` 색키 처리 때문에 `file://`에서 캔버스가
  오염돼 읽을 수 없어 base64로 남겼다(아래 미결 항목). `index.html`은 5.9MB → 11.8KB, `js/assets.js`가 1.3MB다.
- [x] 마커 기준으로 CSS를 `css/` 폴더 파일들로, JS를 `js/` 폴더 파일들로 옮긴다. 마커 순서가 곧 로딩 순서다. `<link>`·`<script src>`를 그 순서대로 나열한다.
  → CSS 6개(계획의 5개 + `overrides.css`), JS 13개(계획의 12개 + `assets.js`). 이유는 기획서 v0918_1에 있다.
- [x] 파일을 나누되 **코드 내용은 바꾸지 않는다**. 전역 참조 방식도 그대로 둔다(모든 파일이 한 전역 스코프를 공유하므로 classic 로딩에서 그대로 동작한다).
  → 이어붙여 원본과 바이트 비교로 확인했다(CSS 126,960 · JS 258,322바이트 동일). 예외 한 곳: `GameArt.init`이
  CSS 커스텀 속성에 넣는 `url()`을 문서 기준 절대 URL로 바꿨다 — CSS가 `css/`로 내려가며 상대 경로가
  `css/assets/...`로 풀려 404가 났다.
- [x] `balance-editor.html`은 `index.html`을 iframe으로 띄우고 `CONFIG`에 접근한다. 분할 후에도 그대로 동작하는지 확인한다.
  → 고치지 않고 동작한다. 입력 423개 생성, `player.atkSpeed` 1→8 적용이 게임 `CONFIG`에 반영됨.
- [x] `.gitattributes`에 webp를 binary로 표시한다.

### 하지 않는 것

- 함수·객체 이름 변경, 죽은 코드 제거, 로직 정리. 전부 다음 세션 이후로.
- `기획서.md` 히스토리 수정. 구현 기술 선택 섹션에 한 줄만 남긴다.

### 완료 조건

- [x] 브라우저에서 로컬 파일로 열어 로비 → 전투 입장 → 한 WAVE 진행 → 중도 종료 → 결과까지 콘솔 에러 없이 된다.
  → 헤드리스 크로미움 430×900, `file://`·`http://` 양쪽. WAVE 3까지 진행 후 종료·결과·로비 복귀. 에러 0.
- [x] 저장 데이터(`slagma.campaign.v4`)가 분할 전과 그대로 읽힌다.
  → 분할 전 빌드로 만든 저장을 분할 후 빌드에 넣어 확인. 16개 키·값 모두 불변.
- [x] `balance-editor.html`이 열리고 값 변경이 게임에 반영된다.
- [x] 이미지 9장이 전부 보인다(로비 배경, 초상, 적 스프라이트 등).
  → 9장 모두 로드되고 fighters 색키 투명도가 `file://`·`http://`에서 동일하다(54.8 / 54.7 / 55.5%).

---

## 세션 2 · 플랫폼 어댑터 + 진입점 분리 + 스모크 스크립트

**목적**: 저장·분석·플랫폼을 얇은 어댑터 뒤로 보내고, 개발용 기능을 출시 진입점에서 뺄 수 있게 하고, 이후 세션의 회귀를 잡을 자동 검사를 하나 만든다.

### 2-1. 어댑터 3종 (`js/platform.js` 하나에 두는 것으로 충분)

세 어댑터 모두 **인터페이스만 정하고 지금은 가장 단순한 구현체**를 꽂는다. 게임 코드가 `localStorage`·SDK·분석 라이브러리를 직접 부르는 곳을 없애는 것이 목표다.

- [ ] **저장 어댑터** `Storage`: `load(key)` `save(key, value)` `remove(key)`. 구현체는 지금의 localStorage 그대로. `CampaignStore`와 `GameAudio`(효과음 설정)가 이걸 쓰게 바꾼다. 나중에 CrazyGames 저장이나 다른 저장소로 바꿀 때 이 파일만 고친다.
- [ ] **분석 어댑터** `Analytics`: `track(name, params)` `progression(status, stage, params)` `setUser(key, value)`. 구현체는 두 개 — 개발용은 콘솔 출력, 출시용은 세션 7까지 no-op. `status`는 `start` `complete` `fail` 세 값으로 고정한다(ByteBrew의 progression 이벤트가 이 형태다).
- [ ] **플랫폼 어댑터** `Platform`: `init()` `loadingStart()` `loadingStop()` `gameplayStart()` `gameplayStop()` `languageHint()`. 구현체는 no-op, `languageHint()`는 `null` 반환. 세션 7에서 CrazyGames 구현체로 바꾼다.
- [ ] 어댑터 선택은 진입점(HTML)에서 한다. 게임 코드는 전역 `Storage` `Analytics` `Platform`만 본다.

### 2-2. 개발/출시 진입점 분리

- [ ] `index.html`은 **출시 진입점**이 된다. 밸런스 에디터 연동·데이터 초기화 버튼·`#action-log` 디버그 표시·개발용 콘솔 로그는 여기서 빠진다.
- [ ] `dev.html`을 **개발 진입점**으로 만든다. `index.html`과 같은 파일들을 로드하고 추가로 개발 기능을 켠다. 방식은 전역 플래그(예: `window.SLAGMA_DEV = true`)를 스크립트 로딩 전에 세팅하고, 게임 코드가 그 플래그로 개발 UI를 켜고 끄는 정도면 된다. 파일을 두 벌 관리하지 않는다.
- [ ] `balance-editor.html`은 iframe 대상을 `dev.html`로 바꾼다.
- [ ] 데이터 초기화는 개발 진입점에만 둔다. (기획서 "데이터 초기화" 항목의 기능 자체는 유지.)

### 2-3. 이벤트 호출 지점 심기

`Analytics`를 부르는 자리를 게임 코드에 지금 넣는다. 실제 전송은 세션 7에서 켜진다. 이벤트 목록은 아래 표를 기준으로 하되, 넣다가 명백히 빠진 것이 보이면 추가하고 표를 갱신한다.

| 이벤트 | 호출 시점 | 파라미터 |
|---|---|---|
| `session_start` | 페이지 첫 로드 | `lang`, `orientation`(portrait/landscape), `isNew`(저장 없음 여부) |
| `tutorial_step` | 튜토리얼 단계 통과 또는 건너뜀 | `step`, `skipped` |
| progression `start` | 전투 입장 | `stage` |
| progression `complete` | 최종보스 처치 | `stage`, `wave`, `durationSec` |
| progression `fail` | 패배 또는 중도 종료 | `stage`, `wave`, `durationSec`, `reason`(defeat/quit) |
| `stage_clear_first` | 새 최고 스테이지 클리어 | `stage` |
| `gacha` | 소환 1회 | `count`(누적 횟수) |
| `level_up` | 수호자·스킬 레벨업 | `kind`(character/skill), `id`, `level` |
| `star_up` | 승급 | `kind`, `id`, `star` |
| `skill_used` | 전투 중 스킬 사용 | `skill`, `wave` |
| `milestone_claim` | 마일스톤 수령 | `id`, `tier` |
| `language_change` | 옵션에서 언어 변경 | `from`, `to` |

- [ ] 위 이벤트를 각 시점에 `Analytics.track` / `Analytics.progression`으로 호출한다. 파라미터 값은 숫자와 짧은 문자열만 쓴다.
- [ ] `session_start`의 `orientation`은 세션 4·5의 레이아웃 판정 함수가 생기면 그 값을 쓴다. 지금은 `innerWidth > innerHeight`로 임시 판정해도 된다.

### 2-4. 스모크 스크립트

- [ ] `tools/smoke.mjs` 하나. Playwright로 `dev.html`을 열어 콘솔 에러·페이지 에러를 수집하고, 로비 → 전투 입장 → 일정 시간(예: 20초) 진행 → 중도 종료 → 결과 화면까지 클릭으로 진행한다. 에러가 하나라도 있으면 실패.
- [ ] 세로(예: 430×900)와 가로(예: 1280×720) 두 뷰포트로 각각 한 번씩 돈다. 세션 4 전에는 가로가 세로 레이아웃 그대로 나오는 게 정상이므로 에러 여부만 본다.
- [ ] 실행 방법을 이 문서 맨 아래 "도구" 절에 적는다. Playwright 설치는 `package.json` 없이 `npx playwright`로 충분하면 그렇게 하고, 필요하면 최소 `package.json`을 둔다. 게임 자체에는 npm 의존성이 생기지 않는다.

### 하지 않는 것

- 문자열 분리(세션 3). 어댑터에 들어가는 메시지도 아직 한글 그대로 둔다.
- ByteBrew·CrazyGames SDK 실제 연결(세션 7).
- 죽은 코드 정리는 어댑터로 대체되며 자연히 사라지는 것만.

### 완료 조건

- [ ] `index.html`과 `dev.html` 둘 다 열리고, `index.html`에는 에디터·초기화·디버그 표시가 없다.
- [ ] 게임 코드에 `localStorage` 직접 호출이 남아 있지 않다(`platform.js` 제외).
- [ ] `dev.html`에서 한 판 돌리면 콘솔에 이벤트 로그가 표의 순서대로 찍힌다.
- [ ] 스모크 스크립트가 세로·가로 둘 다 통과한다.

---

## 세션 3 · 문자열 테이블화 + 언어 시스템

**목적**: 코드 안의 모든 사용자 노출 문자열을 언어 파일로 빼고, 언어를 파일 추가만으로 늘릴 수 있게 한다. 이 세션이 끝나도 화면은 지금과 완전히 같아야 한다(한국어만 있으므로).

현재 규모: JS 안 한글 리터럴 약 560개, HTML·CSS 안 약 180개. 한 세션에 다 못 하면 화면 단위로 끊어서 두 세션으로 한다. 끊는 순서 참고안: 시작·로비·옵션 → 전투 HUD·주문서·스킬 → 결과·마일스톤·소환 → 설명서·튜토리얼 → 데이터(수호자·스킬·마일스톤 이름과 설명).

### 3-1. 구조

- [ ] `js/i18n.js`: 언어 레지스트리와 `t()`. `I18N.register(code, table)`, `I18N.setLanguage(code)`, `I18N.languages()`(등록된 언어 목록), `t(key, params)`.
- [ ] `i18n/ko.js`, (세션 6에서) `i18n/en.js`. 각 파일은 `I18N.register('ko', {...})` 한 번만 부른다. 새 언어 = 파일 하나 + `<script src>` 한 줄.
- [ ] 키는 점 표기 계층(예: `lobby.stamina`, `skill.spear.name`, `manual.section.flow.title`). 키 이름은 영어, 의미 기준으로 짓는다. 화면 이름을 접두어로 쓰되 여러 화면이 공유하는 문자열은 `common.` 아래에 둔다.
- [ ] 자리표시자는 `{name}` 형식. `t('battle.wave', {n: 5})`. CONFIG 수치가 들어가는 설명서·튜토리얼 문장은 전부 자리표시자로 만든다. 수치를 문자열에 직접 박지 않는다.
- [ ] 없는 키는 영어 테이블로 폴백하고, 그것도 없으면 키 자체를 반환한다. 개발 진입점(`SLAGMA_DEV`)에서는 빠진 키를 콘솔에 한 번씩 경고한다.
- [ ] 숫자는 `Intl.NumberFormat(현재 언어)`로 포맷하는 헬퍼 하나를 `i18n.js`에 둔다. 기존 `formatDamage` 같은 함수가 그것을 쓰게 바꾼다.

### 3-2. 데이터 안의 문자열

- [ ] 수호자 이름·패시브 이름, 스킬 이름·설명, 마일스톤 이름·설명, 미사일·적 타입 이름, 희귀도·종족·정체성 이름은 **이름도 키로** 뺀다(인철 확정 2026-09-18: 고유명사도 번역 대상). 데이터 객체는 `nameKey` 같은 키만 갖고 표시 시점에 `t()`를 부른다.
- [ ] 저장 데이터에는 이름이 아니라 ID만 들어가는지 확인한다. 이름이 들어가 있으면 마이그레이션이 필요하므로 이 세션에서 ID로 바꾸고 `CampaignStore.migrate`에 보정을 추가한다.

### 3-3. 언어 선택

- [ ] 언어 설정은 저장 데이터와 **별도 키**(예: `slagma.lang`)에 `Storage`로 저장한다. 데이터 초기화에 영향받지 않는다.
- [ ] 초기 언어 결정 순서: 저장된 값 → `Platform.languageHint()` → `navigator.language` 앞 두 글자가 등록된 언어면 그것 → 기본값. 기본값은 이 세션에서는 `ko`, 세션 6에서 `en`으로 바꾼다.
- [ ] 옵션 UI에 언어 선택을 넣는다. 현재 상단 우측 ❓·⚙(효과음) 버튼 자리에 옵션 팝업을 하나 두고 효과음·언어를 함께 넣는 정도가 적당하다. 목록은 `I18N.languages()`에서 자동 생성하고 각 언어는 자기 이름을 자기 언어로 표시한다(`한국어`, `English`).
- [ ] 언어를 바꾸면 현재 화면을 다시 그린다. 전투 중 변경은 막아도 된다(로비·시작·결과에서만 허용).
- [ ] `<html lang>`을 현재 언어로 바꾼다. 폰트는 이 세션에서는 그대로 두고 세션 6에서 정리한다.
- [ ] `Analytics.track('language_change', ...)` 호출.

### 하지 않는 것

- 영어 번역 작성(세션 6).
- 밸런스 에디터의 한글은 개발 도구이므로 번역하지 않는다.
- `기획서.md`·이 문서는 한국어 그대로.

### 완료 조건

- [ ] 게임 코드(`js/`)와 `index.html`·`dev.html`에 사용자에게 보이는 한글 리터럴이 없다. (주석은 제외.)
- [ ] 화면이 세션 전과 동일하다. 설명서·튜토리얼의 수치가 CONFIG와 여전히 일치한다.
- [ ] 옵션에서 언어 목록에 `한국어` 하나가 보이고 선택이 저장된다.
- [ ] 스모크 통과. 개발 진입점 콘솔에 빠진 키 경고가 없다.

---

## 세션 4 · 전투 화면 가로 레이아웃

**목적**: 데스크톱 16:9와 폰 가로에서 전투 화면이 가로 배치로 나온다. 세로는 지금 그대로.

전투 판정은 440×480 논리 좌표라 기기와 무관하다. 바꾸는 것은 **표시 배치와 캔버스 맞춤 규칙**뿐이다. 판정·밸런스는 손대지 않는다.

### 할 일

- [ ] 레이아웃 판정 함수 하나(예: `Layout.mode()` → `portrait` | `landscape`). 기준은 뷰포트 가로세로 비율. 참고안: 가로가 세로보다 크고 가로 폭이 일정값(예: 700px) 이상이면 `landscape`. 이 함수 하나만 판정하고 CSS는 `<html data-layout="landscape">` 같은 속성으로 따라간다. 미디어쿼리와 JS 판정이 어긋나지 않게 **판정은 한 곳에서만** 한다.
- [ ] 가로 배치 참고안: 왼쪽에 전장 캔버스를 높이 가득, 오른쪽 한 열에 위에서부터 HUD(HP·WAVE·점수) → 주문서 카드 → 머지 보드 → 생성기·에너지 → 스킬바. 오른쪽 열 폭은 보드 2×8이 편하게 들어가는 정도로 잡고, 남는 폭은 캔버스가 쓴다.
- [ ] 캔버스 맞춤: 현재는 세로 기준으로 늘리고 비율 차가 허용 상한(1.18)을 넘으면 줄인다. 가로에서는 **높이 기준**으로 맞추고 폭이 남으면 좌우 여백, 폭이 모자라면 폭 기준으로 줄이는 분기를 추가한다. 허용 상한 값은 기존 CONFIG 값을 그대로 쓴다.
- [ ] 전장 오버레이(에너지, 스킬바, WAVE 배지, 튜토리얼 카드, 보스 경고, 피해 숫자)의 위치가 가로에서도 캔버스 안쪽 좌표 기준으로 맞는지 확인한다. 피해 숫자는 캔버스 표시 비율을 반영하는 기존 `combatLayerPoint`를 그대로 쓰면 될 것이다.
- [ ] 회전·리사이즈 중 런이 끊기지 않는다. 판정 함수를 `resize`·`orientationchange`에서 다시 부르고 배치만 바뀐다.
- [ ] 마우스로 머지 드래그가 되는지 확인한다(포인터 이벤트면 될 것이다). 안 되면 여기서 고친다.
- [ ] `session_start`의 `orientation`을 이 판정 함수 값으로 바꾼다.

### 하지 않는 것

- 로비·시작·결과 화면(세션 5).
- 캔버스 논리 해상도·히트박스·속도 등 CONFIG 판정값 변경.
- 새 아트.

### 완료 조건

- [ ] 1280×720, 1920×1080에서 전투 화면이 가로 배치로 나오고 캔버스가 잘리지 않는다.
- [ ] 430×900 세로는 세션 전과 동일하다.
- [ ] 전투 중 창 크기를 바꿔도 런이 이어지고 에러가 없다.
- [ ] 스모크의 가로 뷰포트에서 한 판이 돈다.

---

## 세션 5 · 로비·시작·결과 화면 가로 레이아웃

**목적**: 나머지 화면이 넓은 화면에서 460px 세로 기둥으로만 보이지 않게 한다.

### 할 일

- [ ] `#app`의 `max-width:460px`를 `landscape`에서 풀고 상한을 넓게(예: 1100px) 잡는다. 세로는 그대로.
- [ ] 로비: 넓을 때 2열 참고안 — 왼쪽에 스테이지·행동력·재화·출전 버튼, 오른쪽에 탭(수호자·스킬·마일스톤·소환) 내용. 수호자 목록 4열은 폭에 따라 6~8열로 늘어나게 `grid` `auto-fill`로 바꾼다.
- [ ] 시작 화면(전투 준비): 수호자 편성과 스킬 확인을 가로로 나란히.
- [ ] 결과 화면: 보상 카드가 화면 가운데에 놓이고 좌우가 비어도 어색하지 않게. 크게 바꿀 필요는 없다.
- [ ] 소환 공개 연출(카드 10장)이 가로에서도 한 화면에 들어오는지 확인.
- [ ] 팝업(옵션·설명서·마일스톤·확인창)의 최대 폭과 위치가 가로에서 자연스러운지 확인.
- [ ] 폰 가로(예: 900×430)에서 세션 4·5 레이아웃이 그대로 작게 보이는지 확인한다. 별도 레이아웃을 만들지 않고, 글자·버튼이 눌리는 크기인지만 본다. 너무 작으면 `landscape` 판정 최소 폭을 올려 폰 가로는 세로 레이아웃으로 두는 것도 선택지다. 어느 쪽이든 이 문서 미결 항목에 결과를 적는다.

### 하지 않는 것

- 새 화면·새 기능.
- 세로 레이아웃 변경.

### 완료 조건

- [ ] 1280×720에서 로비·시작·결과·소환이 가로 배치로 나오고 스크롤 없이 주요 조작이 보인다.
- [ ] 430×900 세로는 세션 전과 동일하다.
- [ ] 스모크 통과.

---

## 세션 6 · 영어화

**목적**: 영어 테이블을 채우고 기본 언어를 영어로 바꾼다.

### 할 일

- [ ] `i18n/en.js` 작성. `ko.js`와 키가 1:1로 같아야 한다. 키 차이를 찾는 간단한 스크립트를 `tools/`에 두면 좋다(두 테이블의 키 집합 비교).
- [ ] 기본 언어를 `en`으로 바꾼다. 브라우저가 한국어면 `ko`로 열리는 것은 세션 3의 결정 순서가 처리한다.
- [ ] 수호자·스킬 이름 영문 표기는 이 세션에서 인철에게 확인받는다. 음차(Miren, Astel)로 갈지 의역으로 갈지. 확인 전까지는 음차로 채워 둔다.
- [ ] 영문이 한글보다 길어 넘치는 곳을 세로·가로 두 레이아웃에서 잡는다. 특히 스킬 버튼, 주문서 카드, 마일스톤 목록, 소환 요약, 튜토리얼 카드. 글자 크기를 줄이기보다 줄바꿈·말줄임을 먼저 쓴다.
- [ ] 폰트: 한국어 폰트(Gothic A1)는 `ko`일 때만 로드되게 한다. 숫자 폰트(Orbitron)는 공통.
- [ ] `<title>`·메타 설명도 언어에 따른다.

### 하지 않는 것

- 제3 언어 추가. 구조만 열어 둔다.
- 설명서 내용 재구성. 번역만.

### 완료 조건

- [ ] 두 테이블의 키가 일치한다.
- [ ] 영어로 세로·가로 한 판씩 돌려 넘침·잘림이 없다.
- [ ] 옵션에서 `English` ↔ `한국어` 전환이 로비·시작·결과에서 즉시 반영된다.
- [ ] 스모크 통과.

---

## 세션 7 · ByteBrew → CrazyGames SDK → 출시 빌드 → QA

**목적**: 어댑터에 실제 구현체를 꽂고 출시 빌드를 확정한다. 순서는 **ByteBrew 먼저**. 이벤트가 대시보드에 실제로 흐르는 것을 본 뒤 플랫폼을 붙이는 편이 디버깅이 쉽다.

### 7-1. ByteBrew

- [ ] ByteBrew 웹(HTML5) SDK를 `index.html`에서만 로드한다. `dev.html`은 콘솔 구현체 유지.
- [ ] `Analytics`의 출시 구현체를 작성한다. `track` → 커스텀 이벤트, `progression` → progression 이벤트, `setUser` → 사용자 속성. **정확한 SDK 함수명은 이 세션에서 ByteBrew 문서를 보고 확정한다.** 이 문서의 함수명을 믿지 말 것.
- [ ] 게임 ID·SDK 키는 `platform.js`가 아니라 `index.html`의 초기화 스크립트에 둔다. 저장소가 비공개라 코드에 직접 넣어도 된다(인철 확인 필요 — 미결 항목).
- [ ] SDK 로드 실패·차단(광고 차단기 등)에도 게임이 멈추지 않는다. 어댑터 안에서 try/catch로 삼킨다.
- [ ] 대시보드에서 `session_start`와 progression 이벤트가 보이는 것을 확인한다.

### 7-2. CrazyGames SDK

- [ ] SDK를 `index.html`에서 로드하고 `Platform` 출시 구현체를 작성한다. `init` → SDK 초기화, `loadingStart/Stop` → 로딩 구간 신호, `gameplayStart/Stop` → 전투 입장·종료, `languageHint` → SDK가 주는 사용자 언어.
- [ ] `gameplayStart`는 전투 입장, `gameplayStop`은 결과 화면 진입에서 부른다. 로비는 게임플레이 아님.
- [ ] 저장은 이번 런치에서 localStorage를 유지한다. CrazyGames 저장으로 바꾸는 것은 미결 항목에 둔다.
- [ ] SDK가 없는 환경(로컬 파일, 다른 호스트)에서도 게임이 동작한다. `Platform` 구현체가 SDK 부재를 감지하면 no-op으로 떨어진다.
- [ ] 광고·결제·로그인은 이번 런치 범위 밖. 기획서 "제외 범위"와 같다.

### 7-3. 출시 빌드 확정

- [ ] `index.html`에 개발 기능이 없는지 다시 확인(에디터·초기화·디버그 로그·콘솔 이벤트 출력·빠진 키 경고).
- [ ] CrazyGames 업로드 형태(zip 안에 `index.html`이 루트)를 만들 수 있도록 포함할 파일 목록을 정한다. `dev.html`·`balance-editor.html`·`tools/`·`기획서.md`·이 문서는 제외. 목록을 이 문서 "도구" 절에 적는다.
- [ ] 자산 경로가 전부 상대 경로인지 확인.

### 7-4. QA 체크리스트

- [ ] 데스크톱 1920×1080 · 1280×720 가로에서 로비·전투·결과·소환 한 바퀴
- [ ] 모바일 세로에서 한 바퀴
- [ ] 폰 가로에서 한 바퀴(세션 5 결정에 따라)
- [ ] 전투 중 새로고침 → 로비로 돌아오고 저장이 유지된다
- [ ] 탭 전환·백그라운드 복귀 후 전투가 이어진다(또는 일시정지 후 재개)
- [ ] 언어 선택이 새로고침 후 유지된다
- [ ] 저장 없는 첫 방문에서 튜토리얼이 뜨고 영어로 나온다
- [ ] 분석 이벤트가 대시보드에 찍힌다
- [ ] 스모크 통과

### 완료 조건

- [ ] QA 체크리스트 전부 통과. 기획서 로드맵 7단계 체크박스를 채운다.

---

## 미결 항목 (이 계획 범위)

작업 중 결정이 필요해진 것을 여기 쌓는다. 확정되면 해당 세션 항목에 반영하고 `기획서.md` 히스토리에도 기록한다.

- ByteBrew 게임 ID·SDK 키를 코드에 직접 넣을지 (저장소 비공개 여부 확인)
- 수호자·스킬 이름 영문 표기 방식 (음차 / 의역)
- 폰 가로를 가로 레이아웃으로 둘지 세로 레이아웃으로 둘지 (세션 5에서 확인 후 결정)
- CrazyGames 저장 API로 전환할지 (이번 런치는 localStorage 유지)
- 스모크 스크립트를 세션 1 끝에 당겨 만들지 (분할 직후가 가장 안전하나 세션 1이 길어짐) → 세션 1에서는 만들지
  않았다. 같은 내용의 임시 스크립트로 검증만 하고 `tools/smoke.mjs`는 계획대로 세션 2에서 만든다.
- `fighters_01~03`을 파일로 뺄지 (세션 1 보류). 뺄 수 있는 조건은 두 가지다 — (1) `file://` 더블클릭 전제를
  놓고 개발 진입점을 로컬 서버로 여는 것(밸런스 에디터는 이미 서버를 요구한다), (2) 추출 시점에 색키를 미리
  구워 알파가 들어간 webp를 만들고 `prepareFighterAtlas`·`keyFighterPixels`를 지우는 것. (2)는 "색키 원본
  픽셀을 다른 엔진에서 재사용하려고 남긴다"는 현재 주석과 충돌하므로 인철 확인이 필요하다. 출시 빌드는
  https로 서비스되니 세션 7에서는 어느 쪽도 걸림돌이 아니다. 1.3MB가 `js/assets.js`에 남아 있다.

## 이 계획에 넣지 않은 것 (로드맵의 다른 단계)

아래는 기획서 로드맵 3·4·6단계다. 이 계획이 끝난 뒤 별도로 잡는다. 이번 작업 중 손대지 않는다.

- 임시 전설 3명의 패시브 교체 (연쇄·폭발·산탄 고유 규칙 설계)
- 골드 소비처 추가, 일일 보상·업적·도감
- 효과음·배경음 (지금은 사인파 비프 1종, 기본 꺼짐)
- 아트·최종 UI
- 첫 이용자 1클릭 전투 진입, 튜토리얼 단순화

## 도구

세션 2 이후 채운다.

- 스모크 실행: (세션 2에서 기록)
- 언어 키 비교: (세션 6에서 기록)
- 출시 빌드 포함 파일: (세션 7에서 기록)
